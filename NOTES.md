# Working notes

> ไฟล์นี้คือ**บันทึกการทำงาน** — ยาว เรียงตามเวลา อ่านตอนต้องการรายละเอียด
> ถ้าอยากได้ภาพรวมว่าเว็บประกอบยังไงและมีขั้นตอนอะไรบ้าง อ่าน
> [ARCHITECTURE.md](ARCHITECTURE.md) · กฎฉบับย่อสำหรับ session ใหม่อยู่ที่ [CLAUDE.md](CLAUDE.md)

## สิ่งที่ผู้เรียนบอกมาแล้ว

- **ภาษาไทยเป็นหลัก** ศัพท์เทคนิคคงไว้เป็นอังกฤษ (slice, pointer, receiver, …)
- **อยากได้ HTML + โจทย์** — เคยตอบเป็นข้อความยาวในแชทแล้วผู้เรียนทักว่า
  "ไม่เห็นมี html หรือโจทย์เลย" → ทุกครั้งที่สอน ต้องออกมาเป็นไฟล์ HTML
  ที่มีแบบฝึกหัด ไม่ใช่คำอธิบายในแชท
- **ชอบรูปแบบ "สรุป + คำอธิบาย"** — บอกเองว่า "ดูสรุปความเข้าใจง่ายดี มีคำอธิบาย"
  → คงสไตล์: อธิบาย *ทำไม* ไม่ใช่แค่ *อะไร*
- **ต้องการโครงสร้างแยกตามวัน** + หน้าแรกที่มี Content Table
  → `index.html` ที่ root เป็นประตูเดียวเข้าทุกอย่าง
- **จะส่งสไลด์วันใหม่มาเรื่อย ๆ** → ระบบต้องขยายได้โดยไม่ต้องรื้อ
- กำลังเรียนคอร์ส **PEA DevPool — Enterprise Go Engineering with AI Integration**

## โครงสร้าง workspace (ต้องรักษาไว้)

```
index.html                    ← ประตูเดียว · แยก FRONT / BACK + progress ของแต่ละฝั่ง
docs.html                     ← ตัวอ่านเอกสาร .md ทุกไฟล์ (?f=<path>)
404.html                      ← Vercel เสิร์ฟให้เองเมื่อไม่พบหน้า
assets/lesson.css             ← ทุกหน้าใช้ร่วมกัน
assets/quiz.js                ← quiz + predict + ปุ่มเฉลย + ติ๊ก "เรียนจบบทนี้"
assets/progress.js            ← ติ๊กบทที่ทำเสร็จในหน้าแรก (localStorage)
assets/md.js                  ← markdown → HTML ใช้โดย docs.html เท่านั้น
assets/demo.js                ← กล่อง "ผลลัพธ์จริง" ของบท HTML/CSS (iframe srcdoc)
day-N/lessons/NNNN-slug.html  ← ฝั่ง BACK (Go) · บท 01-55 · แบ่งตามวัน
day-N/reference/cheatsheet.html
fe-<slug>/lessons/NNNN-slug.html  ← ฝั่ง FRONT · บท 56-78 · แบ่งตามชื่อไฟล์สไลด์
fe-<slug>/reference/cheatsheet.html
        ↑ เลขบทต่อเนื่องกันทั้งเว็บ ห้ามรีเซ็ตตามฝั่ง (เลขบท = คีย์ localStorage)
vercel.json  .gitignore  robots.txt  README.md   ← สำหรับ deploy
from_teacher/                 ← PDF ต้นฉบับจากผู้สอน · gitignore ไว้ ไม่ push ขึ้น repo
```

**เวลาได้สไลด์ชุดใหม่:**

**ข้อ 0 — ตัดสินก่อนว่าเป็นฝั่งไหน:** ชื่อไฟล์สไลด์กำกับวันมาไหม
· กำกับ → ฝั่ง BACK ใช้ `day-N/` · **ไม่ได้กำกับ → ฝั่ง FRONT ใช้ `fe-<slug>/`**
โดย `<slug>` มาจากชื่อสไลด์ · **ห้ามตั้งวันเอง** จะไปชนกับวันจริงที่ส่งมาทีหลัง

1. `mkdir -p <กลุ่ม>/lessons <กลุ่ม>/reference`
2. บทเรียนต่อเลขจากบทสุดท้าย (ตอนนี้จบที่ 0078) — **เลขต่อเนื่องทั้งเว็บ ห้ามรีเซ็ตตามฝั่ง**
   เพราะ `quiz.js` ใช้เลขสี่หลักจากชื่อไฟล์เป็นคีย์ `localStorage`
3. เขียน `<กลุ่ม>/reference/cheatsheet.html` ครอบคลุมสไลด์ทั้งชุด
4. เพิ่ม `<section class="day">` ใน `index.html` (ในบล็อก `#back` หรือ `#front` ให้ถูกฝั่ง)
   + ทุกแถวต้องมี `data-track="back|front"` + อัปเดตตัวเลขใน `.progress-summary`
   + แก้การ์ด `.upcoming` ให้เป็นวันถัดไป
5. แก้ `nav` ของบทสุดท้ายกลุ่มก่อนหน้า ให้ลิงก์มาบทแรกของกลุ่มใหม่
6. **เพิ่มกลุ่มใหม่ลงใน `DAYS` (ฝั่ง back) หรือ `FRONT` (ฝั่ง front) ของ `assets/nav.js`** —
   ถ้าลืม บทใหม่จะเปิดได้จากลิงก์ตรงเท่านั้น **แต่จะไม่โผล่ในเมนูด้านบนเลย**
   และจะไม่มีอะไรฟ้อง เพราะไม่ใช่ลิงก์เสีย · ทุกหน้าใหม่ต้องมีบรรทัดนี้ใน `<head>` ด้วย:
   `<script src="../../assets/nav.js" defer></script>`
   (สคริปต์ฉีดอัตโนมัติแบบรันซ้ำได้อยู่ที่ `scratchpad/inject-nav.py`)
   · กลุ่มใหม่ฝั่ง front ต้องแจกเลข `d` แล้วใส่เลขเดียวกันใน `FE_GROUP_ID` ของ
   `tools/build-search-index.py` ด้วย — **ลืมแล้วตัวสร้าง index จะหยุดพร้อมบอกชื่อกลุ่ม**
   · บท HTML/CSS ที่ใช้กล่อง `.demo` ต้องมี `<script src="../../assets/demo.js" defer></script>` ด้วย
7. **รันสองสคริปต์นี้ตามลำดับ** แล้ว commit ผลด้วย:
   ```
   python3 tools/add-heading-ids.py        # แปะ id ให้ h2/h3 ของหน้าใหม่
   python3 tools/build-search-index.py     # สร้าง assets/search-index.json ใหม่
   ```
   ทั้งคู่ idempotent · ตัวหลังมี `--check` สำหรับตรวจอย่างเดียว
   **ถ้าไปแทรก `<h2>` กลางบทเก่า** `add-heading-ids.py` จะ exit 1 เพราะเลข id จะซ้ำ —
   ให้รันด้วย `--renumber` (แล้วเช็คว่าไม่มีใครลิงก์ `#sn-sec-N` เดิมไว้)
8. รัน link checker ก่อนปิดงาน — ต้องตรวจ `?f=` ของ `docs.html` ด้วย
   ไม่ใช่แค่ path
9. **`git add . && git commit && git push`** — ผู้เรียนสั่งไว้ (18 ส.ค. 2026)
   ว่าให้ push เองเลยทุกครั้งที่เพิ่มวันใหม่ **ไม่ต้องถามก่อน** ·
   push ขึ้น `main` แล้ว Vercel deploy อัตโนมัติ · รายงานพร้อม commit hash

**ไม่ต้องทำเอง:** ปุ่ม "ทำบทนี้เสร็จแล้ว" ท้ายบท — `assets/quiz.js`
อ่านเลขบทจากชื่อไฟล์ (`^\d{4}-`) แล้วแทรกการ์ดเหนือ `<nav class="nav">` ให้เอง
หน้าที่ชื่อไฟล์ไม่ขึ้นต้นด้วยเลขสี่หลัก (cheat sheet) จะถูกข้าม

**เพิ่มเอกสาร `.md` ใหม่:** เขียนไฟล์ตามปกติ แล้วเพิ่มหนึ่งบรรทัดในอาร์เรย์
`DOCS` ของ `docs.html` · ตัว renderer อยู่ที่ `assets/md.js` รองรับเท่าที่เอกสาร
ชุดนี้ใช้จริง (heading / ตาราง / code / list / blockquote / hr)
**ห้ามเขียน HTML ปนลงไฟล์ `.md`** ถ้าต้องใช้ syntax ใหม่ให้ไปเพิ่มใน `md.js`

## ข้อสังเกตในการสอน

- ผู้เรียนสังเกตเห็นเองว่าอะไรขาดหาย → ตรวจสอบตัวเองเก่ง เหมาะกับ
  predict-the-output มากกว่าอ่านคำอธิบายยาว ๆ
- ยังไม่มีหลักฐานว่าเข้าใจเรื่องไหนแล้วบ้าง — บททั้ง 18 จึงเป็น retrieval practice
  ถ้าทำ quiz ผ่านเกิน 80% ให้เร่งความยาก
- **Day 4 เป็นวันแรกที่สอนการตัดสินใจเชิงออกแบบ ไม่ใช่ไวยากรณ์** →
  predict-the-output เปลี่ยนแนวเป็นถาม "มี error ไหม" แทน "ได้ค่าอะไร"
  เพราะกับดักของวันนี้พังแบบเงียบ ๆ (ดู `learning-records/0004`)
- **Day 5 ทำให้ผู้เรียนตรวจงานตัวเองได้แล้ว** → ตั้งแต่บท 19 เป็นต้นไป
  ภารกิจควรลงท้ายด้วย **"เขียนเทสพิสูจน์"** แทน "ลองรันดู"
  (ดู `learning-records/0005`)
- สไลด์ที่เป็น **API surface ล้วน ๆ** (รายการ method/flag/assert) →
  ยกรายการทั้งหมดไป cheat sheet แล้วให้บทเรียนโฟกัสที่ **"เลือกอันไหนเมื่อไหร่"**
  เพราะรายการจำไม่ได้จากการอ่าน แต่การเทียบคู่จำได้
- **Day 6 เนื้อหาเกินกรอบ 5 บท** → แบ่งบทตาม **"จำนวนกับดักที่ต้องรันให้ดู"**
  ไม่ใช่ตามหัวข้อในสไลด์ · ได้ 9 บท บทละ 13–18 นาที (ดู `learning-records/0006`)
- **ตั้งแต่ Day 6 ทุกบทที่เจอ deadlock ต้องชี้ไปที่ `goroutine N [...]`** —
  ผู้เรียนมีจุดเริ่มวินิจฉัยที่แน่นอนจุดเดียว ดีกว่าจำอาการทีละแบบ ·
  ตาราง "แปลคำในวงเล็บ" อยู่ใน cheat sheet Day 6 หัวข้อ 11
- **Day 8 แบ่งเป็นสองครึ่งที่คนละธรรมชาติ** — บท 33–36 **ไม่มีโค้ด Go เลย**
  (REST · status code · query/body/header · กฎตั้งชื่อ) เพราะการเลือก `404` หรือ `409`
  ไม่ใช่เรื่องของภาษา · บท 37–42 เป็นโค้ดล้วน · ถ้าเอาสองครึ่งปนกัน ผู้เรียนจะจำ API
  ของ Gin ได้แต่ยังออกแบบ endpoint ไม่เป็น (ดู `learning-records/0009`)
- **ตั้งแต่บท 37 ทุกบทที่มี handler ต้องลงท้ายด้วย `httptest`** — เป็นวิธีเดียว
  ที่ทำให้ภารกิจ "เขียนเทสพิสูจน์" (กฎตั้งแต่ Day 5) ยังใช้ได้เมื่อโค้ดกลายเป็น HTTP server
- **สอน `net/http` ให้จบก่อนแตะ Gin เสมอ** — สไลด์แสดง net/http แค่ครึ่งหน้าแล้วข้ามไป Gin
  ถ้าทำตามสไลด์ ผู้เรียนจะไม่มีวันรู้ว่า framework ทำอะไรให้ · บท 38 จึงเป็น
  **การรีแฟกเตอร์โค้ดบท 37 ทีละบรรทัด** ไม่ใช่บทสอน Gin ตั้งแต่ศูนย์
- **`go vet ./...` เลื่อนขั้นเป็นคำสั่งประจำวันตั้งแต่ Day 6** —
  เป็นวันแรกที่ vet จับบั๊กที่ compiler จับไม่ได้ถึง 3 แบบ
  (copy lock · lostcancel · stdversion)

## Backlog บทเรียนถัดไป

1. ~~**`net/http` + routing**~~ — **ทำแล้วในบท 37** (ServeMux + method pattern
   Go 1.22+ + `r.PathValue`) และบท 38 (Gin) · ช่องว่างที่ใหญ่ที่สุดปิดแล้ว
2. ~~**`httptest`**~~ — **ทำแล้วในบท 40** ครบทั้ง `NewRecorder`, table-driven,
   `assert.JSONEq` และการเรียก handler ตรง ๆ โดยไม่ผ่าน router
3. ~~**เขียนเทสให้ repository ที่ต่อ database จริง**~~ — **ทำแล้วในบท 29–30**
   ด้วย Testcontainers (container จริง ไม่ใช่ DB ที่ลงในเครื่อง)
4. **Closure + first-class function** — ยังอยู่ใน cheat sheet Day 3 อย่างเดียว
   รอดูว่าภารกิจ `trackTime` (วงเล็บ 2 ชุด) ในบท 08 ผ่านไหม ถ้าไม่ผ่าน → ทำทันที
5. **Struct embedding & promotion** — ควรมีบทที่ให้ทำนายว่า method ไหนถูกเรียก
   เมื่อชื่อชนกัน (Cat.Eat vs Animal.Eat) · ตอนนี้มีแค่ *interface* embedding
   ในบท 11 ยังไม่มี *struct* embedding
6. ~~**Encapsulation ข้าม package**~~ — **ทำแล้วในบท 27** (`internal/`)
   พร้อมหลักฐานว่า compiler บล็อกจริงข้าม module
7. **`json.RawMessage` + custom `UnmarshalJSON`** — สัญญาไว้ท้ายบท 13 แล้วว่า
   ถ้าเจอ JSON ที่โครงสร้างไม่แน่นอนให้ถามมา
8. **Workshop ท้ายสไลด์** ที่ยังไม่ได้ทำเป็นบทเรียน:
   Employee Management (Day 3), Inventory system (Day 3),
   ระบบสมาชิก Gold/Silver/Bronze (Day 2)
9. **`for range` กับ map/slice เชิงลึก** — ยังกระจายอยู่หลายบท อาจรวมเป็นบทเดียว
10. **Benchmark (`testing.B`) + `testing.F` (fuzzing)** — ไม่มีในสไลด์
    แต่อยู่ใน package `testing` เดียวกัน · หลัง Day 6 น่าสนใจขึ้นมาก
    เพราะใช้วัดผล worker pool ได้จริง (ต่อยอดจากบท 26)
11. **`sync.Once` · `errgroup` · `atomic`** — ไม่มีในสไลด์ Day 6 แต่เจอแน่ในโค้ดจริง ·
    `errgroup` คือ WaitGroup ที่รวบ error ได้ ต่อยอดจากบท 21+25 ตรง ๆ
12. ~~รอสไลด์ Day 7~~ — **มาแล้ว ทำเป็นบท 28–32**
    (Mockery · Testcontainers · Goose · Redis) · ครึ่งแรกของสไลด์ซ้ำกับบท 27 จึงข้าม
13. ~~รอสไลด์ Day 8~~ — **มาแล้ว ทำเป็นบท 33–42** (REST · net/http · Gin · Swagger) ·
    ครึ่งแรกของสไลด์ (Cache/Redis) ซ้ำกับบท 32 จึงข้าม เหลือแค่ `SET … EX` กับ `GETDEL`
    ที่ยกไปไว้ใน cheat sheet
14. **`http.Server` + timeout + graceful shutdown** — บท 37 ใช้ `http.ListenAndServe`
    ซึ่งไม่มี timeout สักตัว · production ต้องใช้ `&http.Server{ReadTimeout, WriteTimeout}`
    + `srv.Shutdown(ctx)` ต่อยอดจาก `context` ในบท 25 ตรง ๆ
15. **pagination ที่ทำถูกวิธี** — บท 35 สอน `?page=&limit=` แต่ยังไม่ได้พูดถึง
    cursor-based ที่ไม่พังเมื่อข้อมูลถูกแทรกระหว่างหน้า
16. **auth จริง ๆ (Keycloak / JWT)** — สไลด์ Day 8 มีแค่ endpoint ใน API spec
    (`/login` `/callback` `/exchange` `/logout`) ยังไม่มีเนื้อหา · น่าจะมาใน Day 9
17. **rate limiting · structured logging (`log/slog`)** — ไม่มีในสไลด์
    แต่เป็นสองอย่างแรกที่จะต้องเพิ่มทันทีที่ API ขึ้นจริง
18. ~~รอสไลด์ Day 8 ฉบับเต็ม~~ — **ได้ (Post) 91 หน้ามาแล้ว** เทียบทีละหน้ากับ (Pre) 74 หน้า
    พบของใหม่ 17 หน้าใน 3 ช่วง (40–48 · 75–76 · 85–90) ทำเป็นบท **43–44**
19. ~~**library อ่าน config**~~ — **มาแล้วใน Day 9** ทำเป็นบท 45–48
    (`caarlos0/env` · `godotenv` · `validator/v10` · `internal/config`) ·
    บท 44 อัปเดตกล่องปิดท้ายให้ลิงก์ไปต่อแล้ว
20. **`DOCKER_HOST` / `TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE` สำหรับ colima/Podman** —
    ยังไม่มีในบท 29/30 ทั้งที่เครื่องที่ใช้ทำคอร์สนี้เป็น colima และ agent ชนเรื่องนี้เองตอนตรวจ
21. ~~**Mid Exam**~~ — **ทำแล้วในบท 55** · Day 9 ฉบับ (Post) เพิ่มหน้าโจทย์มาอีกหนึ่งหน้าเต็ม
    (รวมเป็น 9 ข้อ) บทนี้จึงอ่านโจทย์ทีละคำแทนการเฉลยโค้ด เพราะมันคือข้อสอบของผู้เรียน
22. **middleware ตรวจ JWT ใน Gin** — Day 9 สอนว่า token มาจากไหน แต่ยังไม่มีบทที่เขียน
    middleware ดึง Bearer token มา verify แล้วยัด user ลง context

## เรื่องที่ต้องถามผู้เรียน (ยังค้าง)

- [ ] **ยืนยัน MISSION.md** — ยังเป็นฉบับร่างที่อนุมานเอง ถามไปแล้วยังไม่ได้คำตอบ
- [ ] สนใจเข้าชุมชน (r/golang, Gophers Slack, Golang Thailand) ไหม
- [ ] จบคอร์สแล้วเอาไปทำอะไร (REST API? CLI? data pipeline?) —
      คำตอบนี้เปลี่ยนโจทย์ในทุกบทเรียนให้ตรงงานจริง

## เรื่องเทคนิค

- **ตัวอย่างโค้ดทุกอันต้องรันจริงก่อนเผยแพร่** — สคริปต์ทดสอบอยู่ที่ scratchpad
  `gocheck/` (Go 1.26.5 ผ่าน mise) เจอจุดที่สไลด์ผิดมาแล้ว 11 จุด
  (ดู `learning-records/0002`) · Day 5 ต้องลง testify ด้วย:
  `go get github.com/stretchr/testify && go mod tidy`
- **Docker บนเครื่องนี้คือ `colima` ไม่ใช่ Docker Desktop** — `docker context` ที่ active
  คือ `colima` (Ubuntu 24.04 · Docker Server 29.5.2 · 6 CPU · RAM 8 GB) ·
  เครื่องมี context ของ desktop-linux กับ rancher-desktop ค้างไว้ด้วยแต่ไม่ได้ใช้
  ⚠️ **testcontainers ต้องตั้ง `TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock`
  คู่กับ `DOCKER_HOST`** ไม่งั้น Ryuk ต่อ socket ไม่ติด — ยังไม่มีเรื่องนี้ในบทเรียน 29/30
- **Day 6 ตรวจ GORM กับ PostgreSQL 17 จริงผ่าน Docker** (เครื่องนี้มี docker พร้อมใช้):
  `docker run -d --name <ชื่อ> -e POSTGRES_PASSWORD=... -p 55432:5432 postgres:17`
  แล้ว **ลบ container ทิ้งเมื่อตรวจเสร็จ** · ใช้ port 55432 กันชนกับของผู้เรียน
- **โค้ดฝั่ง "วิธีเก่าที่แย่" ในสไลด์ต้องรันด้วยเสมอ** — Day 6 เจอ 2 จุดที่พังจริง
  กลายเป็น predict-the-output ที่ดีที่สุดของบท 19
- ⚠️ **ผลตรวจจาก subagent ห้ามเชื่อทันทีเรื่อง "สไลด์ผิด"** (บทเรียนจาก Day 7) —
  agent รายงานมา 21 จุด คัดแล้วจริง 5 จุด · มันเห็นแต่ผลรันของตัวเอง ไม่ได้อ่านสไลด์ทั้งหน้า
  จึงเก่งเรื่อง *กับดักของเครื่องมือ* แต่ตัดสิน *สไลด์เขียนว่าอะไร* ไม่ได้
  **→ ทุกจุดที่จะลง learning record 0002 ต้องรันซ้ำเองและเปิดสไลด์เทียบเอง**
  · **ผิดได้สองทาง** (ยืนยันตอนตรวจซ้ำ 21 ส.ค. 2026): agent รายงานว่าบล็อก eviction ของบท 32
  เป็น MISMATCH เพราะมันรัน 5 ครั้งได้ผลไม่ตรง แต่ผมรันตามบทเป๊ะ ๆ แล้ว**ตรงทุกบรรทัด** —
  LRU ของ Redis เป็น approximate ผลจึงแกว่งตามวิธียัดข้อมูล · agent เปลี่ยนเงื่อนไขเองแล้วสรุปว่าบทผิด
- **ตรวจซ้ำให้กวาดทั้งบท อย่าตรวจเฉพาะจุดที่สงสัย** — รอบตรวจซ้ำบท 30-32
  ชุดที่สงสัยไว้ 21 จุดไม่มีอันไหนเป็นข้อผิดพลาดเชิงการสอนเลยตามที่ประเมินไว้
  แต่พอกวาดทั้ง 72 บล็อก **เจอข้อผิดพลาดเชิงการสอน 1 จุดที่อยู่นอกชุดที่สงสัย**
  (บท 30 อ้างว่า `flag.Parse()` ใน `TestMain` ใช้ไม่ได้ ซึ่งผิด — `testing.Init()`
  ลงทะเบียน `-test.*` ตั้งแต่ใน `MainStart` ก่อน `TestMain` แล้ว)
- **ก่อนยิง workflow ยาว ๆ ให้ `node --check` สคริปต์ก่อน** — Day 7 ลืมประกาศตัวแปร
  ทำให้เฟส Write พังทั้งหมด · กู้ได้ด้วย `resumeFromRunId` (ผลตรวจที่แพงถูก cache ไว้)
  แต่เสียเวลารอบหนึ่งฟรี ๆ
- ⚠️ **brief ที่เราเขียนให้ agent ก็ผิดได้** (บทเรียนจาก Day 8) — ผมเขียนใน brief ว่า
  "`/users/:id` ชนกับ `/users/me` แล้ว Gin panic" ซึ่ง**ผิด** · agent เฟส Verify รันจริง
  ได้ `OK (no panic)` แล้วไปรันซ้ำบน gin v1.9.1 ยืนยันอีกรอบ · agent ที่เขียนบทเลือกเชื่อผลรัน
  ไม่เชื่อ brief แล้วเอาความเข้าใจผิดนั้นไปทำเป็น predict-the-output เสียเลย
  **→ ประโยคที่กันไว้ได้คือ "ถ้าผลจริงต่างจากที่คำถามคาดไว้ ให้รายงานตามจริง อย่าดัดให้ตรงคำถาม"**
  ต้องมีในคำสั่งของ agent ตรวจทุกครั้ง
- **แยกเฟส Verify ออกจาก Write ได้ผลกว่าให้ agent ตัวเดียวทำทั้งคู่** (Day 8) —
  Day 7 ให้ agent ตรวจกับเขียนพร้อมกัน มันรายงาน "สไลด์ผิด" 21 จุด จริง 5 จุด ·
  Day 8 แยกเฟส (5 ตรวจ → 10 เขียน → 10 audit) agent ที่ตรวจไม่มีแรงจูงใจจะสรุปแทนคนเขียน
- **ข้อห้าม "ห้ามแต่ง output" ต้องเขียนในคำสั่งของ audit ด้วย ไม่ใช่แค่ตอนเขียน** —
  จาก 25 จุดที่ auditor เลือกไม่แก้ มี 8 จุดที่เหตุผลคือ *"แก้แล้วเท่ากับปลอม output"*
  (เลขบรรทัดใน `go test`, ชื่อ module ที่รันคนละโฟลเดอร์, `0.493s` vs `0.494s`)
- **`swag` CLI กับ `github.com/swaggo/swag` ใน `go.mod` ต้องเป็นรุ่นใกล้กัน** —
  `gin-swagger v1.6.1` ลาก library v1.8.12 มา แต่ CLI `@latest` เป็น v1.16.4 →
  `unknown field LeftDelim` คอมไพล์ไม่ผ่าน · แก้ด้วย `go get github.com/swaggo/swag@latest`
- เพิ่ม component ใหม่ให้ใส่ใน `assets/` **อย่า inline ในบทเรียน**
- **ปุ่ม `.reveal` ใช้ได้ทั้งใน `.predict` และ `.task`** (เพิ่มตอน Day 6) —
  contract: `button.reveal` + `div.answer` เป็นลูกของ card **หนึ่งปุ่มต่อหนึ่ง card**
  · `quiz.js` ผูกจากตัวปุ่มขึ้นไปหา `closest('.predict, .task')`
  ⚠️ ตอน Day 6 เคยใส่ปุ่มใน `.task` ก่อนแก้ `assets/` → เฉลยโผล่ทันทีและปุ่มกดไม่ได้
  **ถ้าจะใช้ component เดิมในที่ใหม่ ต้องเช็ค CSS + JS ว่ารองรับก่อน**
- **`.ask` เป็น `<p class="ask">` แทรกได้หลายจุดในบท ไม่ใช่ `<div>` ก้อนเดียวท้ายบท** —
  ตอน Day 8 เขียน brief ผิดเป็น `<div class="ask">` ก่อน `nav` · agent ทำตามของจริงในไฟล์
  (บท 32 มี 3 จุด) แล้วรายงานว่า brief ไม่ตรง — ถูกแล้ว **ยึดไฟล์จริงเป็นสัญญา ไม่ใช่ brief**
- **"ตัวเลือกที่ N" ในคำอธิบาย quiz นับเริ่มที่ 1** (ตรงกับที่ผู้เรียนเห็น)
  ส่วน `data-answer` นับเริ่มที่ 0 — สองอย่างนี้ตั้งใจให้ต่างกัน อย่าไปไล่แก้
- quiz: ตัวเลือกทุกข้อควรยาวเท่ากัน (คำ/ตัวอักษร) ไม่ให้เดาได้จากรูปแบบ
- ทุกบทต้องมี: predict-the-output ≥1 · quiz 4 ข้อ · ภารกิจ 1 ชุด ·
  แหล่งอ้างอิงหลัก 1 แหล่ง · คำเชิญให้ถามกลับ

- **CSS: `height: 100%` ในกล่องที่มีพี่น้องอยู่ข้างบน = ระเบิดเวลา** — `.compare .side > pre`
  เคยตั้ง `height: 100%` ซึ่งคิดจากความสูงของ `.side` **ทั้งก้อน (รวม `<h4>`)**
  `<pre>` เลยล้นทับข้อความข้างล่างพอดีเท่าความสูง `<h4>` (วัดได้ 39px) ใน 33 ไฟล์ ·
  ของแบบนี้ต้องใช้ flex (`flex: 1 1 auto`) ที่คิดจาก**ที่ว่างที่เหลือ** ไม่ใช่ความสูงทั้งก้อน
- **วิธีตรวจ layout ทั้งเว็บโดยไม่ต้องเปิดดูทีละหน้า** (ไฟล์ harness เป็นไฟล์ชั่วคราว ไม่ได้ commit
  ไว้ในรีโป — ทำใหม่ได้ใน 5 นาที): เสิร์ฟโฟลเดอร์ด้วย `python3 -m http.server` แล้วทำหน้า HTML
  หน้าเดียวที่ไล่โหลดทุกหน้าใส่ `<iframe>` ทีละหน้า จากนั้นวนทุก element เทียบ
  `getBoundingClientRect()` ของลูกกับของแม่ ถ้าลูกล้นเกินขอบล่าง/ขวาของแม่เกิน 3px
  และแม่เป็น `overflow: visible` ให้รายงาน
  <br>ต้องข้าม `position: absolute/fixed/sticky` และข้าม `<td>`/`<th>` (rowspan ทำให้ล้นโดยธรรมชาติ
  = false positive) · **ต้องรันหลายความกว้าง** — bug `<code>` ไม่ตัดบรรทัด โผล่เฉพาะที่ 380px
  ที่ 768px ขึ้นไปไม่เห็นเลย · รอบล่าสุดตรวจ 55 หน้า × 3 ความกว้าง (380/768/1280) เหลือ 0 จุด
- **ทดสอบ detector ด้วยการเอา bug กลับมาใส่เสมอ** — ก่อนเชื่อว่า "ตรวจแล้วไม่เจอ"
  ต้องเอา CSS เก่ากลับมาทับแล้วดูว่า detector ร้องไหม · ไม่งั้นแยกไม่ออกระหว่าง
  "ไม่มีปัญหา" กับ "เครื่องมือเสีย"
- **ผลรันที่ agent ส่งกลับมา อาจเป็นของจริงบนเครื่องที่มี wrapper** — เครื่องนี้มี wrapper
  ครอบ shell อยู่ ทำให้ `diff` ที่ไฟล์เหมือนกันพิมพ์ `[ok] Files are identical`
  ทั้งที่ `diff` ตัวจริง**ไม่พิมพ์อะไรเลย** · agent copy มาใส่บทเรียนตรง ๆ เกือบหลุด ·
  เวลาจะโชว์ "ไฟล์เหมือนกัน" ให้ผู้เรียน **ใช้ `md5` ดีกว่า `diff`** เพราะมันพิมพ์ค่าออกมาให้เห็น
  ส่วน `diff` ที่เงียบ หน้าตาแยกไม่ออกจาก "คำสั่งไม่ได้รัน"
- **จะสรุปว่า flag/ตัวแปรไหนเป็นเหตุ ต้องเปลี่ยนทีละตัว** — บท 41 เคยสรุปผิดว่า
  `--parseInternal` ทำให้ชื่อ definition เปลี่ยน เพราะทดสอบโดยใส่คู่กับ `--parseDependency`
  ความจริงคือ `--parseDependency` ทำคนเดียว · ผิดแบบนี้ขึ้นเว็บไปแล้วรอบหนึ่ง

- **กล่อง `.analogy` และ `.jargon` — ลำดับ 3 ขั้นที่ใช้กับทุกบท** อธิบายภาษาคน →
  เทียบให้เห็นภาพ (`.analogy` ต้องมีท่อน "ที่ไม่เหมือนกัน" เสมอ) → ติดป้ายศัพท์ (`.jargon`)
  เพื่อให้ผู้เรียนเอาไปคุยกับ senior/AI ได้ · เกณฑ์: จำนวน `.jargon` ต้องไม่น้อยกว่า `.analogy`
  และอย่างน้อย 2 กล่องต่อบท
- **การเขียนให้ "ง่ายขึ้น" ทำให้ผิดง่ายกว่าการเขียนโค้ด** — โค้ดผิด compiler ฟ้อง
  แต่การเปรียบเทียบที่ผิดไม่มีใครฟ้อง · รอบเติมกล่องครั้งแรกมี 33 จุดที่ผิดจริง เช่น
  กล่องเขียน `int(3.9)` ได้ `3` ทั้งที่คอมไพล์ไม่ผ่าน และขัดกับกล่องอื่นในไฟล์เดียวกัน ·
  **กฎ: ความถูกต้องมาก่อนความง่ายเสมอ ถ้าเปรียบเทียบแล้วเลี่ยงความเข้าใจผิดไม่ได้ ให้ตัดทิ้ง**
- **โค้ดที่โชว์เป็น "ไฟล์" ต้องมี `package` + `import` ครบ** — Day 9 รอบแรกไม่มีเลยสักบท
  แล้วยังเขียนกำกับว่า "ก๊อปไปวางแล้วคอมไพล์ผ่าน" · มือใหม่เจอ `undefined: time` ตั้งแต่ก้าวแรก ·
  เกณฑ์ตรวจ: ก๊อปทุกก้อนที่อ้างว่าเป็นไฟล์ไปวางแล้ว `go build` ต้องผ่านจริง
- **agent ตั้งชื่อไฟล์ไม่ตรงที่สั่งได้** — Day 9 เพี้ยน 4 ไฟล์ ทำให้ nav ขาด 3 เส้น ·
  ต้องเขียนใน brief ว่า "ชื่อไฟล์ต้องเป๊ะ ห้ามเปลี่ยน" และรัน `linkcheck.js` ทันทีที่เขียนเสร็จ
- **agent ทิ้งขยะไว้ในรีโปได้** — เจอโฟลเดอร์ชื่อ `W=` ที่ root (เกิดจาก `W=$(mktemp -d)` ที่ shell
  ตีความพลาด) และ container/volume ที่ไม่ได้ลบ · **ก่อน commit ทุกครั้งต้องเช็ค
  `git status --porcelain | grep "^??"` และ `docker ps -aq`**
- **ตัวตรวจรายงาน false blocker ได้** — เคยแจ้งว่า `checkhtml.js` ไม่มีอยู่จริงเพราะไป
  `find` หาในรีโป ทั้งที่ไฟล์อยู่ใน scratchpad · ต้องบอก path เต็มใน brief

### บทเรียนจาก Day 9 (Post) — 25 ส.ค. 2026

- **"config สองบรรทัดตีกัน → ลบอันไหนก็ได้" เป็นคำตอบที่สมมาตรเกินจริง**
  ตอน Day 9 ผมพิสูจน์ว่า `KC_HOSTNAME_STRICT` เป็น no-op แล้วหยุดตรวจ
  ที่ต้องทำต่อคือถามว่า "แล้วบรรทัดที่ *มี* ผล ถ้าเอาออกจะเกิดอะไร"
  → ต้องรัน **สองรอบ** (เอา A ออก / เอา B ออก) ไม่ใช่รอบเดียว

- **การทดสอบผ่านเบราว์เซอร์ที่ `localhost` ปิดตาเรื่อง hostname ทั้งหมด**
  `KC_HOSTNAME` ที่ปักหมุดไว้กับที่ไม่ปัก ให้ผลเหมือนกันเป๊ะบน `localhost`
  ความต่างโผล่ตอนเรียกด้วย **ชื่ออื่น** เท่านั้น (`127.0.0.1`, ชื่อ service ใน compose)
  → เวลาตรวจอะไรที่เกี่ยวกับ URL/host ให้ยิงด้วย `Host:` header หลายแบบเสมอ

- **⚠️ กับดักของ layout sweep: browser cache ทำให้ "ผ่าน" แบบปลอม**
  รอบนี้ผมแก้ `lesson.css` ให้กลับไปเป็นเวอร์ชันที่มีบั๊กเพื่อทดสอบตัวตรวจ
  แล้ว sweep รายงาน **0 จุด** ทั้งที่บั๊กอยู่ในไฟล์จริง —
  เพราะเบราว์เซอร์ยังใช้ CSS ตัวเก่าใน memory cache (ตรวจได้ด้วยการเทียบ
  `document.styleSheets[0].cssRules` กับ `fetch(css, {cache:'no-store'})`)
  → **ต้องเสิร์ฟด้วย `Cache-Control: no-store` และตัด `Last-Modified`/`ETag` ทิ้ง**
  สคริปต์เซิร์ฟเวอร์อยู่ที่ `scratchpad/nocache.py`
  พอแก้แล้วตัวตรวจจับได้ทันที **648 จุด · ค่า `bottom` = 39px** ซึ่งคือความสูงของ `<h4>` พอดี
  → ทุกครั้งที่รายงานว่า "0 จุด" **ต้องทดสอบตัวตรวจด้วยการใส่บั๊กกลับเข้าไปก่อน**

- **detector ที่เทียบ `getBoundingClientRect()` ของ inline ใน inline ให้ผลขยะ**
  รอบแรกได้ 1,057 จุดซึ่งเป็น `code in strong` / `code in em` ล้วน ๆ —
  เพราะ rect ของ inline element คือกรอบรวมของทุกบรรทัดที่มันพาดผ่าน
  → ข้ามเมื่อ `display` ของตัวเองหรือของพ่อขึ้นต้นด้วย `inline`

### Navbar (เพิ่ม 26 ส.ค. 2026)

`assets/nav.js` ไฟล์เดียวจบ — ข้างในมีทั้ง DATA · CSS (inject เป็น `<style>`) · โค้ดสร้าง DOM
เหตุผลที่รวมเป็นไฟล์เดียว: ทุกหน้าเพิ่มแค่ `<script>` บรรทัดเดียว ไม่ต้องเพิ่ม `<link>` อีกเส้นใน 67 ไฟล์

สิ่งที่ต้องรู้ถ้าจะไปแก้ต่อ:
- **`404.html` ต้องใช้ `/assets/nav.js` แบบ root-absolute** ไม่ใช่ relative
  เพราะ Vercel เสิร์ฟหน้านี้จาก URL ไหนก็ได้ (เช่น `/day-9/lessons/typo.html`)
  ผมฉีดผิดเป็น relative รอบแรก จับได้ตอนจำลองเสิร์ฟจาก path ลึก
- `lesson.css` **ไม่มี `z-index` เลยแม้แต่ตัวเดียว** ทั้งไฟล์ · navbar ใช้ 100 จึงชนะทุกอย่าง
- `lesson.css` มี selector เปล่า ๆ ที่ leak ใส่ navbar ได้: `a{}` (สีม่วง+ขีดเส้นใต้) ·
  `ul/ol/li{}` (bullet + indent) · `button{}` ใน `@media print` ที่เป็น `!important` ·
  และ **class `.nav` ถูกใช้ไปแล้ว** โดยแถบ prev/next ท้ายบท
  → ทุก class ของ navbar จึงขึ้นต้นด้วย `sn-`
- แถบกินเต็มความกว้างด้วย `margin: 0 -1.25rem` แล้วเติม padding คืนข้างใน —
  ไม่ทำให้หน้าเลื่อนแนวนอน เพราะความกว้างสุทธิเท่าเดิม (ยืนยันด้วย sweep 67 หน้า × 3 ความกว้าง)
- `scroll-margin-top` ของหัวข้อ **วัดจากความสูงแถบจริงตอน runtime** ไม่ได้ hardcode
- **dropdown เปิดด้วย `click` ห้ามใช้ `:hover`** — มือถือไม่มี hover ถ้าใช้จะกดไม่ได้ทั้งเครื่อง

### ช่องค้นหาใน navbar (เพิ่ม 26 ส.ค. 2026)

สามส่วน: `assets/search-index.json` (ข้อมูล) · `tools/build-search-index.py` (ตัวสร้าง) ·
ส่วนที่ 4.5 ใน `assets/nav.js` (UI)

**คลังคำมาจากไหน** — ไม่ได้ index เนื้อหาเต็ม (4.9 MB) แต่เก็บ 4 ชนิด:
ศัพท์ 617 (จาก `<dt>` ในกล่อง `.jargon`) · หัวข้อ 884 (h2/h3) · บทเรียน 64 · โค้ด 1,863
รวม 3,428 รายการ = 397 KB (gzip 96 KB) **โหลดตอนโฟกัสช่องครั้งแรกเท่านั้น**

สิ่งที่ต้องรู้ถ้าจะแก้ต่อ:
- **หัวข้อทุกอันมี `id` จริงในไฟล์แล้ว** (`sn-sec-N` สำหรับ h2 · `sn-sub-N` สำหรับ h3 · 960 อัน)
  เลขตั้งให้ตรงกับที่ `nav.js` เคยคำนวณตอน runtime พอดี dropdown "ในบทนี้" จึงไม่เปลี่ยนพฤติกรรม
  · cheat sheet มี slug เดิมของตัวเอง 104 อัน **ห้ามทับ** เพราะ `<ol class="toc">` ลิงก์อยู่
- **id ถูกนับตามตำแหน่ง** ถ้าแทรก h2 กลางบทเก่าจะทำให้เลขของอันหลัง ๆ เลื่อนทั้งหมด →
  สคริปต์จะ exit 1 แทนที่จะเขียนทับเงียบ ๆ ต้องสั่ง `--renumber` เอง
- **แถวโค้ดมี 1,863 รายการ** ถ้ารู้สึกว่าผลค้นรก จุดที่ปรับคือเกณฑ์ "ปรากฏ ≥2 ครั้ง" ในตัวสร้าง
- ranking แบ่งเป็นชั้น (ตรงเป๊ะ / ขึ้นต้น / อยู่กลาง / อยู่ในคำอธิบาย) ห่างกันชั้นละ 1000
  แล้วค่อยบวก KIND_BONUS (ศัพท์ 480 · บท 320 · หัวข้อ 160 · โค้ด 0)
  → ในชั้นเดียวกัน ศัพท์ชนะโค้ดเสมอ แต่ **ข้ามชั้นไม่ได้** เช่นค้น `keycloak`
  โค้ด `keycloak:` ที่ตรงเป๊ะยังชนะชื่อบทที่แค่ขึ้นต้นตรง — ตั้งใจให้เป็นแบบนั้น
- **ห้ามใช้ `innerHTML` ในส่วนนี้เด็ดขาด** ข้อความในคลังมาจาก HTML ของบทเรียน
  มี `<`, `&`, เครื่องหมายคำพูดปนอยู่ 379 รายการ · ตอนนี้ประกอบด้วย DOM API ล้วน
  ไฮไลต์ใช้ `<mark>` ที่สร้างด้วย `createElement`
- เส้นแบ่งจอแคบของช่องค้นหาคือ **1000px ไม่ใช่ 767px** — วัดจริงที่ 768px แล้วปุ่ม Day 1-9
  กินไป 696 จาก 728px ที่ใช้ได้ เหลือที่พอสำหรับปุ่ม 🔍 เท่านั้น ถ้ายัด input จะล้น

### ฝั่ง Frontend (เพิ่ม 27 ส.ค. 2026)

สไลด์สองไฟล์: `[PEA] Basic HTML_CSS.pdf` (74 หน้า) · `[PEA] Functional_Programming_with_TypeScript.pdf` (48 หน้า)
ทำเป็นบท **56–62** (HTML/CSS) และ **63–68** (TypeScript)

- **สไลด์ฝั่งนี้ไม่ได้กำกับวันมาให้** จึงแบ่งกลุ่มตามชื่อไฟล์สไลด์ (`fe-html-css`, `fe-ts-fp`)
  ไม่ใช่ตามวัน · ถ้าตั้งวันเองจะไปชนกับวันจริงที่อาจส่งมาทีหลัง
- ⚠️ **สไลด์เป็นภาพเกือบทั้งหมด** — `pdftotext` ได้ 15 KB จาก 74 หน้า
  โค้ดตัวอย่าง**ทั้งหมด**อยู่ในรูป ต้อง `pdftoppm -jpeg -r 100` แล้วให้ agent เปิดรูปอ่าน
  · เกณฑ์ที่ใช้ตัดสิน: **ข้อความที่แกะได้น้อยกว่า ~500 ไบต์ต่อหน้า = ต้องแปลงเป็นรูป**
- **บท CSS ที่ไม่มีผลลัพธ์ให้ดู = บทที่พิสูจน์อะไรไม่ได้เลย** จึงทำ `assets/demo.js` ขึ้นมา
  เรนเดอร์โค้ดชุดเดียวกับที่โชว์ ใน `<iframe srcdoc>` ที่ `lesson.css` เข้าไม่ถึง
  · ⚠️ ต้องติดคลาส `is-live` **ก่อน** `appendChild` เพราะ `.demo:not(.is-live)` เป็น
  `display:none` — ใส่ iframe ตอนแม่ยังซ่อนอยู่ ได้กล่องกว้าง 0 แล้ววัดความสูงได้ 442px
  จากข้อความบรรทัดเดียว (เจอตอนทดสอบจริง)
  · ⚠️ วัดความสูงต้องวัดจาก `body.scrollHeight` ไม่ใช่ `documentElement` เพราะ `<html>`
  ยืดเต็มความสูง iframe เสมอ กล่องจึงไม่มีวันหดลง
- **กล่อง `.demo` ที่เกิดในที่กว้าง 0 จะระเบิดถ้าไม่กัน** — auditor เจอตอนเปิดบท 58
  ในแท็บที่ pane กว้าง 0: กล่อง `data-h="180"` ยืดเป็น 578px · ผมทดสอบเองแล้วยืนยัน —
  **ปิดการ์ดออก ได้ 512px · ใส่การ์ดกลับ ได้ 52px** (ข้อความบรรทัดเดียวเท่ากัน)
  → `fit()` ต้อง `if (!frame.clientWidth) return;` และมี `ResizeObserver` คอยวัดใหม่
  ตอนกล่องได้ความกว้างจริง · **ทดสอบตัวตรวจด้วยการปิดการ์ดออกก่อนเชื่อว่าแก้แล้ว**
- **id ซ้ำข้าม `<template>` ไม่ใช่บั๊ก** — แต่ละ template ไปอยู่คนละ iframe = คนละ document
  ตัวตรวจต้องนับ id แยกต่อ template ไม่งั้นจะร้องผิดทุกบทที่มีฟอร์มสองตัวอย่าง

### ⚠️ ไฟล์ใน scratchpad หายกลางรอบ workflow (เจอ 5 ก.ย. 2026)

รอบทำบท Next.js เขียน brief ไว้ที่ `scratchpad/BRIEF.md` แล้วให้ agent ทั้ง 20 ตัวอ่าน
**กลางรอบไฟล์นั้นหายไปจาก scratchpad** (พร้อมกับ `verify.py` และ `mksweep.py`)
agent ที่เริ่มทีหลังจึงอ่านเกณฑ์ไม่ได้ · ผลที่เห็น: บท 74 ได้ quiz 3 ข้อ (เกณฑ์คือ 4)
และ jargon น้อยกว่า analogy · บท 76 ไม่มีกล่องภารกิจเลย

**กันไว้ยังไง:**
- **ตรวจว่าไฟล์ brief ยังอยู่** ก่อนยิง workflow รอบถัดไป และหลังจบทุกเฟส
- ถ้าจะให้ชัวร์กว่านั้น ให้ **ฝัง checklist ลงใน prompt ของ agent ตรง ๆ** ไม่ใช่แค่ชี้ไปที่ไฟล์
  (prompt อยู่ในสคริปต์ workflow ซึ่งถูกเซฟไว้ในโปรเจกต์ ไม่หาย)
- อาการที่บอกว่าเกิดเรื่องนี้: **บทที่เขียนทีหลังคุณภาพต่ำกว่าบทแรก ๆ อย่างเป็นระบบ**
  ไม่ใช่สุ่ม ๆ

### ⚠️ `overflow-wrap` ที่ `code{}` ไม่พอ — `<dt>` ของ `.jargon` ก็ต้องมี (เจอ 5 ก.ย. 2026)

sweep ที่ 380px เจอบท 71 มี `scrollWidth` 452 เกิน `clientWidth` 380 อยู่ 72px
ไล่หาแล้ว**ไม่ใช่ element** — เป็น **text node** ในกล่อง `.jargon`:
`React.ButtonHTMLAttributes<HTMLButtonElement>` ที่ไม่มีจุดให้ตัดบรรทัดเลย

`lesson.css` มี `overflow-wrap: anywhere` ที่ `code{}` อยู่แล้ว (พร้อมคอมเมนต์อธิบายบั๊กชุดนี้)
แต่ `.jargon dt` ใส่ `font-family: var(--font-code)` เองโดยไม่ได้ห่อ `<code>` จึงไม่ได้กฎนั้น
→ เติม `overflow-wrap: anywhere` ที่ `.jargon dt` แล้ว sweep กลับเป็น 0

**วิธีไล่หาเวลาเจอ `scrollWidth` เกินแต่ไม่มี element ไหนล้น:**
เดิน `TreeWalker(SHOW_TEXT)` แล้ววัด `Range.getClientRects()` ของแต่ละ text node
ข้ามอันที่มีบรรพบุรุษ `overflow-x != visible` — ตัวการมักเป็นคำยาวคำเดียวที่ตัดบรรทัดไม่ได้

### ⚠️ agent สั่ง pkill กว้างเกินไป ปิด dev server ของผู้ใช้ (เจอ 5 ก.ย. 2026)

agent ตรวจบท 76 เก็บกวาดด้วย `pkill -f "next-server"` → ปิด `next dev` ของโปรเจกต์
`wongnok-web` ของผู้ใช้ที่รันมา 2 ชม. 49 นาที ไปด้วย · agent รายงานเองตามตรง
และ workflow ตีธง `Blocked by classifier` ให้

**กฎที่ต้องเขียนใน brief ทุกครั้งที่ให้ agent เปิด process:**
- ปิดด้วย **PID ที่จดไว้ตอนเปิด** เท่านั้น
- ถ้าต้องใช้ pattern ให้ผูกกับ **path ของ sandbox ตัวเอง** ห้ามใช้ชื่อโปรแกรมลอย ๆ
- **ห้ามฆ่า process ที่อยู่นอกโฟลเดอร์ตัวเอง** — เครื่องนี้มีงานจริงของผู้ใช้รันอยู่

### ⚠️ sandbox ที่ใช้ร่วมกัน ทำให้ build ของคนอื่นพังตามไปด้วย (เจอ 5 ก.ย. 2026)

รอบ Next.js เตรียม `create-next-app` ไว้ชุดเดียวให้ agent 10 ตัวใช้ร่วมกัน
โดยแบ่งโฟลเดอร์กันคนละ `app/tNN/` — **แต่ `next build` มัน build ทั้งโปรเจกต์**
ตอนผมทดสอบเอง ได้ error จากไฟล์ที่ agent บท 78 กำลังเขียนค้างอยู่ ทั้งที่ไม่เกี่ยวกับโค้ดผม

- ข้อความ error บอก path ชัด (`./components/t78/CustomerList.tsx`) จึงพอแยกออกว่าของใคร
- **แต่เสี่ยงที่ agent จะไปแก้ของที่ไม่ได้พัง** หรือสรุปว่าโค้ดตัวเองผิด
- **ครั้งหน้า: ก็อป sandbox ให้ agent คนละชุด** (`cp -R` แล้ว symlink `node_modules`)
  หรือสั่งให้ตรวจแบบ scope แคบแทน `next build` ทั้งโปรเจกต์

### ⚠️ hook ของ rtk กลืน output ของ `npx tsc` (เจอ 27 ส.ค. 2026)

auditor ของบท 64 รายงานว่า `npx tsc --version` คืนข้อความ **"TypeScript: No errors found"**
แทนที่จะคืนเลขเวอร์ชัน — คือคำสั่งถูก hook เขียนใหม่แล้ว output ดิบหายไปทั้งหมด
ต้องเรียกผ่าน **`rtk proxy npx tsc …`** ถึงจะได้ผลจริง

- `tsc` ตรง ๆ (binary ใน PATH) ใช้ได้ปกติ — ผมรันเองแล้วได้ `TS18048`, `TS2552` ครบ
  แต่ `tsc --version` ก็โดนกลืนเหมือนกัน ส่วน `tsc -v` ไม่โดน
- **เข้าข่ายกับดักเดิมที่เคยเจอกับ `diff`** (wrapper พิมพ์ `[ok] Files are identical`
  ทั้งที่ `diff` จริงเงียบ) — เครื่องนี้มี wrapper ครอบ shell อยู่
- **กฎ:** ก่อนเอา output ของเครื่องมือใด ๆ ไปใส่บทเรียน ให้ยืนยันด้วยคำสั่งที่
  **รู้ว่าต้องได้อะไร** (เช่น `--version`) ก่อนหนึ่งครั้ง · ถ้าได้ข้อความแปลก ๆ
  ให้เรียกซ้ำผ่าน `rtk proxy`

### Navbar สองฝั่ง (เพิ่ม 27 ส.ค. 2026)

`nav.js` มองเว็บเป็น 2 ฝั่ง (`TRACKS`) แต่ละฝั่งมีหลายกลุ่ม (`groups`)
ฝั่ง BACK สร้างจาก `DAYS` เดิม · ฝั่ง FRONT จาก `FRONT` ใหม่ · รูปทรงเดียวกันผ่าน `group()`

- **สร้างแถวปุ่มของทั้งสองฝั่งไว้พร้อมกันแล้วซ่อนอันที่ไม่ใช้** ไม่ใช่รื้อสร้างใหม่ตอนกดสลับ
  เพราะ `makeToggle()` เก็บคู่ `{btn, panel}` ไว้ในอาเรย์ `toggles` เพื่อบังคับ "เปิดได้ทีละเมนู"
  รื้อ DOM ทิ้งแล้วอาเรย์จะเหลือ node ที่หลุดจากหน้าไปแล้ว
- `.sn-daysgrp` ใช้ `display:contents` เพื่อให้ปุ่มลูกยังเป็น flex item ของ `.sn-days` เหมือนเดิม
  **ต้องเขียน `[hidden]{display:none}` แยกด้วย** เพราะ `display:contents` ทับกฎ `[hidden]` ของ UA
- **เส้นแบ่ง "ยุบเป็นปุ่ม ☰" ขยับจาก 768px → 1100px** เพราะแถบบนมีตัวสลับฝั่งเพิ่มมา 116px
  วัดจริงที่ 1280px: 🏠 32 + tracks 116 + ปุ่ม Day 1-9 485 + ช่องค้นหา 192 + docs 69 = 894
  จอ 1000px มีที่ให้ 920 → ชนพอดีเกินไป จึงตั้งที่ 1100 · **ค่านี้ต้องตรงกับ `MQ_NARROW`
  ในโค้ดช่องค้นหา** ไม่งั้น 🔍 กับ ☰ จะโผล่คนละจังหวะ
- ⚠️ **เลข `d` ห้ามเอาไปต่อกับคำว่า "Day" ตรง ๆ** — ผลค้นหาเคยขึ้นว่า **"Day 101"**
  เพราะ `whereText()` เขียน `'Day ' + pg.d` ไว้ · แก้ด้วยตาราง `GROUP_BY_D`
  ที่ map เลข → ชื่อกลุ่ม (`Day 6` · `HTML & CSS` · `TypeScript`)
  **เจอตอนทดสอบค้นจริงเท่านั้น** เพราะ dropdown กับแถบ chip ใช้ `g.where` อยู่แล้วจึงถูกมาตลอด
- ฝั่งที่เลือกจำไว้ใน `go-course-track-v1` **แต่ถ้าอยู่หน้าบทเรียน ให้ใช้ฝั่งของหน้านั้นเสมอ**
- เลขกลุ่ม `d` (BACK = เลขวัน · FRONT = 101, 102) อยู่**สองที่**: `FRONT` ใน `nav.js`
  และ `FE_GROUP_ID` ใน `tools/build-search-index.py` — ตัวสร้าง index จะหยุดพร้อมบอกชื่อกลุ่ม
  ถ้าเจอ `fe-*` ที่ยังไม่มีเลข

## Deploy (เพิ่มตอนขึ้น GitHub + Vercel)

repo: <https://github.com/Thuwanon01/learnGo> · เว็บเป็น static ล้วน
**ไม่มี build step ไม่มี dependency** — Vercel เดา preset "Other" เองได้
push ขึ้น `main` แล้ว deploy อัตโนมัติ · branch อื่นได้ Preview URL

- **`from_teacher/` ถูก gitignore** — เป็นสื่อของ PEA DevPool ไม่ใช่ของเรา
  และ 58 MB (Day 6 ไฟล์เดียว 25 MB) · index.html จึงไม่มีลิงก์ไปโฟลเดอร์นั้นแล้ว
  ถ้าจะเปลี่ยนใจ ลบบรรทัดสุดท้ายใน `.gitignore`
- **`vercel.json` มีแค่ 2 เรื่อง**: cache header · เสิร์ฟ `.md` เป็น
  `text/plain; charset=utf-8` (ไม่งั้นเบราว์เซอร์ดาวน์โหลดไฟล์แทนที่จะเปิดอ่าน)
- **`cleanUrls` ตั้ง `false` โดยตั้งใจ** — ลิงก์ภายในทั้ง 782 เส้นลงท้าย `.html`
  ถ้าเปิด clean URL ทุกลิงก์จะโดน redirect 308 ก่อนเสมอ ช้าโดยไม่ได้อะไร
- ⚠️ **`docs.html` ใช้ `fetch()` จึงเปิดด้วย `file://` ไม่ได้** (CORS)
  หน้าอื่นเปิดตรง ๆ ได้หมด · ถ้าจะดู docs ในเครื่อง ให้รัน
  `python3 -m http.server 8000` ก่อน · ตัวหน้าจับกรณีนี้แล้วขึ้นข้อความบอกทางออก
  ไม่ปล่อยให้จอว่าง
- ความคืบหน้าอยู่ใน `localStorage` ของแต่ละคน **ไม่มี backend ไม่มีบัญชี**
  → เพื่อนแต่ละคนมีความคืบหน้าของตัวเอง แต่ไม่ sync ข้ามเครื่อง (ยอมรับข้อนี้)
  คีย์ `go-course-progress-v1` ถูกใช้ทั้ง `progress.js` และ `quiz.js`
  **แก้ที่ไหนต้องแก้ทั้งสองที่**
