# macOS
.DS_Store

# Vercel
.vercel

# สไลด์ต้นฉบับของผู้สอน — ไม่ push ขึ้น repo สาธารณะ
#   1) เป็นสื่อการสอนของ PEA DevPool ไม่ใช่ของเรา
#   2) รวมกัน ~58 MB (Day 6 ไฟล์เดียว 25 MB) ทำให้ clone ช้าโดยไม่จำเป็น
# เพื่อนที่เรียนคอร์สเดียวกันมีไฟล์ชุดนี้อยู่แล้ว
# ถ้าเป็น repo ส่วนตัวและอยากเก็บไว้ด้วย ให้ลบบรรทัดล่างนี้ทิ้ง
from_teacher/
