# ARCHITECTURE — เว็บนี้ประกอบขึ้นมายังไง และทำไมถึงเป็นแบบนี้

เอกสารนี้ตอบสามคำถาม: **หลักการออกแบบระบบ** (design logic) ·
**สัญญาของแต่ละชิ้นส่วน** (contract ที่ห้ามผิด) · และ
**ขั้นตอนแปลงสไลด์หนึ่งวันให้กลายเป็นบทเรียน** (pipeline)

เขียนไว้เพื่อให้ session ใหม่เปิดไฟล์เดียวแล้วทำงานต่อได้ ไม่ต้องอ่านทั้งรีโป

| ถ้าอยากรู้เรื่อง | อ่านที่ไหน |
|---|---|
| กฎที่ห้ามผิด + ทำงานยังไงในรีโปนี้ | [CLAUDE.md](CLAUDE.md) — สั้น โหลดทุก session |
| หลักการ + สัญญา + pipeline | ไฟล์นี้ |
| บันทึกการทำงาน · backlog · กับดักที่เคยเจอจริง | [NOTES.md](NOTES.md) — ยาว อ่านตอนต้องใช้ |
| เรียนไปเพื่ออะไร (ใช้ตัดสินว่าจะสอนอะไรต่อ) | [MISSION.md](MISSION.md) |
| เกิดอะไรขึ้นในแต่ละรอบใหญ่ | `learning-records/NNNN-*.md` |

---

## 1 · หลักการตั้งต้น 6 ข้อ

หกข้อนี้เป็นเหตุผลเบื้องหลังเกือบทุกการตัดสินใจในรีโป ถ้าจะแหก ต้องรู้ตัวว่าแหก

**1.1 static ล้วน ไม่มี build step ไม่มี dependency**
เปิด `index.html` ด้วยเบราว์เซอร์ก็ใช้ได้ครบ (ยกเว้น `docs.html` ที่ต้องใช้ `fetch`)
ผลที่ตามมา: **ไม่มีอะไรไล่อ่านโฟลเดอร์ให้ตอน deploy** ข้อมูลอย่าง "เว็บนี้มีบทอะไรบ้าง"
จึงต้องเขียนตายตัวไว้ในไฟล์ (`DATA` ใน `nav.js`) และมี "ตัวสร้าง" แยกต่างหากใน `tools/`
ที่คนรันเองก่อน commit — ไม่ใช่ตอน deploy

**1.2 หนึ่งบท = หนึ่งไฟล์ HTML ที่จบในตัว**
บทเรียนไม่ import อะไรกันเอง ลบบทหนึ่งทิ้งแล้วบทอื่นไม่พัง (เสียแค่ลิงก์)
เหตุผล: ผู้เรียนกลับมาเปิดทีละบท ไม่ได้อ่านเรียงทั้งเว็บ

**1.3 ของที่ใช้ซ้ำต้องอยู่ใน `assets/` ห้าม inline ในบทเรียน**
มาจากกติกาของ skill `/teach` โดยตรง · เพิ่ม component ใหม่ = เพิ่มใน `assets/`
แล้วบทเรียนอ้างถึง · **ก่อนเอา component เดิมไปใช้ในที่ใหม่ ต้องเช็คก่อนว่า CSS + JS รองรับ**
(เคยพลาดมาแล้ว: เอาปุ่ม `.reveal` ไปใส่ใน `.task` ก่อนแก้ `assets/` → เฉลยโผล่ทันทีและปุ่มกดไม่ได้)

**1.4 ปิด JS แล้วต้องยังอ่านได้**
JS ทำได้แค่ *เพิ่ม* ของ: ตรวจ quiz · เปิดเฉลย · แถบนำทาง · ช่องค้นหา
เนื้อหา โค้ด ตาราง หัวข้อ และ `id` ของหัวข้อ อยู่ใน HTML จริงทั้งหมด

**1.5 ทุกอย่างที่โชว์เป็น "ผลรัน" ต้องรันจริงก่อน**
ห้ามแต่ง output ห้ามเขียนจากความจำ · ถ้าโค้ดก้อนไหนบอกว่าเป็น "ไฟล์"
ต้องมี `package` + `import` ครบและ `go build` ผ่านจริง
ระหว่างทำเจอจุดที่สไลด์ผิดมาแล้วหลายสิบจุด — บันทึกไว้ที่ `learning-records/0002`

**1.6 ความถูกต้องมาก่อนความง่ายเสมอ**
โค้ดผิด compiler ฟ้อง แต่**คำเปรียบเทียบที่ผิดไม่มีใครฟ้อง**
ถ้าเปรียบเทียบแล้วเลี่ยงความเข้าใจผิดไม่ได้ **ให้ตัดทิ้ง** อย่าดันต่อ

---

## 2 · ผังไฟล์และหน้าที่

```
index.html              ประตูเดียวเข้าทุกอย่าง · Content Table ทุกวัน + แถบความคืบหน้า
docs.html               ตัวอ่าน .md ทุกไฟล์ในรีโป (?f=<path>) · มีอาร์เรย์ DOCS อยู่ข้างใน
404.html                Vercel เสิร์ฟเองเมื่อไม่พบหน้า · ⚠️ ต้องใช้ path แบบ /root-absolute

assets/lesson.css       สไตล์ของทุกหน้า · นิยาม CSS variable ทั้งชุด (dark mode + โหมดพิมพ์)
assets/quiz.js          quiz · ปุ่มเฉลย · scorebar · ติ๊ก "เรียนจบบทนี้"
assets/progress.js      ติ๊กบทที่ทำเสร็จในหน้าแรก
assets/nav.js           แถบนำทางบนสุด + ตัวสลับฝั่ง + ช่องค้นหา · มี DATA ของทั้งคอร์สอยู่ข้างใน
assets/demo.js          กล่อง "ผลลัพธ์จริง" ของบท HTML/CSS (เรนเดอร์ใน <iframe>)
assets/md.js            markdown → HTML · ใช้โดย docs.html เท่านั้น
assets/search-index.json  คลังคำค้น · เป็นไฟล์ที่ "สร้างขึ้น" ห้ามแก้มือ

day-N/lessons/NNNN-slug.html     ฝั่ง BACK (Go) · แบ่งตามวันที่เรียน
day-N/reference/cheatsheet.html
fe-<slug>/lessons/NNNN-slug.html ฝั่ง FRONT · แบ่งตาม "ชื่อไฟล์สไลด์" ไม่ใช่ตามวัน
fe-<slug>/reference/cheatsheet.html
        ↑ เลขบทเรียนต่อเนื่องกันทั้งเว็บ (global) ไม่แยกตามฝั่ง — ดูหัวข้อ 4

tools/add-heading-ids.py       แปะ id ให้ h2/h3 ทุกไฟล์ · idempotent
tools/build-search-index.py    สร้าง search-index.json · idempotent · มี --check

MISSION.md RESOURCES.md GLOSSARY.md NOTES.md learning-records/   ← โครงของ skill /teach
from_teacher/           PDF ต้นฉบับจากผู้สอน · gitignore ไว้ · ห้าม commit เด็ดขาด
```

**ตอนนี้มี HTML 82 ไฟล์** = บทเรียน 68 + cheat sheet 11 + `index` + `docs` + `404`
(ฝั่ง BACK บท 01–55 · ฝั่ง FRONT บท 56–68)

---

## 3 · สัญญาของ component (ผิดแล้วพังเงียบ)

CSS อยู่ใน `assets/lesson.css` · พฤติกรรมอยู่ใน `assets/quiz.js`
**ยึดไฟล์จริงเป็นสัญญา ไม่ใช่เอกสารนี้** — ถ้าสองอย่างขัดกัน ไฟล์ถูก แล้วมาแก้เอกสาร

### 3.1 กล่องเนื้อหา

| markup | ใช้ตอนไหน |
|---|---|
| `<div class="note">` | ข้อมูลเสริม เช่น เวอร์ชันที่ใช้ตรวจ |
| `<div class="warn">` | กับดัก · จุดที่สไลด์ผิด |
| `<div class="win">` | ของที่ทำถูกแล้ว · จุดที่ผู้สอนแก้ให้แล้ว |
| `<div class="analogy"><span class="label">…</span>` | เทียบเรื่องเทคนิคกับของในชีวิตจริง |
| `<div class="jargon"><span class="label">…</span><dl><dt>ศัพท์</dt><dd>…</dd></dl>` | ติดป้ายศัพท์ **หลัง**อธิบายจบแล้ว |
| `<div class="compare"><div class="side bad">…</div><div class="side good">…</div></div>` | เทียบโค้ดสองฝั่ง |
| `<div class="diagram">` | แผนภาพหน่วยความจำ (ทำด้วยตัวอักษร) |
| `<div class="table-wrap"><table>…` | **ตารางทุกอันต้องมี wrap** ไม่งั้นจอแคบล้น |
| `<div class="demo" data-h="160"><template>…</template></div>` | เรนเดอร์ HTML/CSS ให้เห็นผลจริง — ดู §3.6 |
| `<ul class="sources">` | แหล่งอ้างอิงท้ายบท |
| `<nav class="nav">` | แถบ prev / หน้าแรก / cheat sheet ท้ายบท |

ระบายสีโค้ดใช้ `<span>` แปะมือ: `.k` keyword · `.s` string · `.c` comment ·
`.n` number/type · `.f` ชื่อฟังก์ชัน — **ไม่มี syntax highlighter** เป็น JS

### 3.2 quiz — ตรวจทันทีที่กด

```html
<div class="quiz" data-answer="2">
  <p class="q">คำถาม</p>
  <div class="opts">
    <button class="opt">ก</button>
    <button class="opt">ข</button>
  </div>
  <div class="explain"><p>ทำไม…</p></div>
</div>
```

- `data-answer` **นับเริ่มที่ 0** · แต่คำว่า "ตัวเลือกที่ N" ในคำอธิบาย **นับเริ่มที่ 1**
  (ตรงกับที่ผู้เรียนเห็นบนจอ) — **สองอย่างนี้ตั้งใจให้ต่างกัน อย่าไปไล่แก้ให้เหมือนกัน**
- ตัวเลือกทุกข้อควรยาวใกล้เคียงกัน ไม่ให้เดาคำตอบจากรูปแบบได้
- ⚠️ **ห้ามสลับลำดับตัวเลือกหรือแก้ `data-answer` เพราะ "คิดว่าเฉลยผิด"**
  ต้องพิสูจน์ด้วยผลรันจริงก่อนเสมอ

### 3.3 predict-the-output และปุ่มเฉลย

```html
<div class="predict">
  <pre><code>…</code></pre>
  <p class="ask">คิดว่าได้อะไร?</p>
  <button class="reveal">เฉลย</button>
  <div class="answer">…</div>
</div>
```

`quiz.js` ผูกปุ่มด้วย `btn.closest('.predict, .task')` →
**ใช้ใน `<div class="task">` ได้ด้วย** แต่ **หนึ่งปุ่มต่อหนึ่ง card เท่านั้น**
· `.ask` เป็น `<p>` แทรกได้หลายจุดในบท ไม่ใช่ก้อนเดียวท้ายบท

### 3.4 scorebar — หนึ่งอันต่อหน้า วางท้าย `<body>`

```html
<div class="scorebar">
  <span class="count"></span>
  <span class="track"><span class="fill"></span></span>
  <span class="pct"></span>
</div>
```

### 3.5 `.demo` — กล่องผลลัพธ์จริง (บท HTML/CSS เท่านั้น)

```html
<div class="demo" data-h="160">
  <template>
    <style>.row { display: flex; gap: 8px }</style>
    <div class="row"><div>A</div><div>B</div></div>
  </template>
</div>
```

`assets/demo.js` ย้ายเนื้อใน `<template>` ไปเรนเดอร์ใน `<iframe srcdoc>` แล้ววัดความสูงจริง

- **ต้องเป็น iframe** เพราะ `lesson.css` ตั้งสไตล์ `p`/`a`/`ul`/`button`/`input` ไว้หมดแล้ว
  ถ้าเรนเดอร์ในหน้าตรง ๆ ตัวอย่างจะโดนสไตล์ของหน้าทับจน**สอนผิด**
- **ต้องอยู่ใน `<template>`** ไม่งั้น CSS ในกล่องรั่วออกมาทั้งหน้าตอน JS ปิด
- sandbox ให้แค่ `allow-same-origin` → `<script>` ในกล่องไม่ทำงาน (ตั้งใจ)
- ⚠️ `demo.js` ต้องติดคลาส `is-live` **ก่อน** `appendChild` เสมอ เพราะ
  `.demo:not(.is-live)` เป็น `display:none` — ใส่ iframe ตอนแม่ยังซ่อนอยู่จะได้กล่องกว้าง 0
  แล้ววัดความสูงได้เป็นค่ามหาศาล
- ปิด JS แล้วกล่องไม่โผล่ (ตั้งใจ) เพราะโค้ดชุดเดียวกันอยู่ในกล่อง `<pre>` เหนือมันอยู่แล้ว

### 3.6 สิ่งที่ **ไม่ต้อง**เขียนเอง

การ์ด "ทำบทนี้เสร็จแล้ว" — `quiz.js` อ่านเลขบทจากชื่อไฟล์ (`^\d{4}-`)
แล้วแทรกให้เหนือ `<nav class="nav">` เอง · หน้าที่ชื่อไฟล์ไม่ขึ้นต้นด้วยเลขสี่หลัก
(cheat sheet) จะถูกข้ามไปเฉย ๆ

---

## 4 · ระบบชื่อและ namespace

**`id` ของหัวข้อ** — `sn-sec-N` สำหรับ `<h2>` · `sn-sub-N` สำหรับ `<h3>` นับตามลำดับที่ปรากฏ
สร้างด้วย `tools/add-heading-ids.py` (ตอนนี้ 960 อัน) ใช้เป็นเป้าของ dropdown "ในบทนี้"
และของผลค้นหา

> ⚠️ **id นับตามตำแหน่ง** ถ้าแทรก `<h2>` กลางบทเก่า เลขของอันหลังจะเลื่อนทั้งหมด
> สคริปต์จะ `exit 1` แทนที่จะเขียนทับเงียบ ๆ · ต้องสั่ง `--renumber` เอง
> แล้วเช็คว่าไม่มีใครลิงก์ `#sn-sec-N` เดิมไว้
>
> cheat sheet มี slug ของตัวเองอยู่แล้ว 104 อัน **ห้ามทับ** เพราะ `<ol class="toc">` ลิงก์อยู่

**คลาสของ navbar ทุกตัวขึ้นต้นด้วย `sn-`** เพราะ `lesson.css` จองชื่อสามัญไว้เยอะมาก
(`.nav` `.lesson` `.label` `.meta` `.track` …) โดยเฉพาะ `.nav` ที่ `quiz.js` ใช้หา
"แถบท้ายหน้า" — ถ้าแถบบนชื่อ `.nav` ด้วย กล่องติ๊ก "เรียนจบแล้ว" จะไปโผล่บนสุดของหน้า

**สองฝั่ง (track) กับกลุ่ม (group)** — `nav.js` มองทั้งเว็บเป็น 2 ฝั่ง:

| ฝั่ง | กลุ่มคืออะไร | โฟลเดอร์ | บท |
|---|---|---|---|
| `back` | หนึ่งวันที่เรียน | `day-N/` | 01–55 |
| `front` | หนึ่งไฟล์สไลด์ | `fe-<slug>/` | 56–68 |

ฝั่ง Frontend แบ่งตามชื่อสไลด์เพราะ**อาจารย์ไม่ได้กำกับวันมาให้ในชื่อไฟล์**
ส่วน**เลขบทเรียนต่อเนื่องกันทั้งเว็บ** ไม่แยกตามฝั่ง — เพราะเลขบทคือคีย์ของ
`localStorage` (อ่านจากชื่อไฟล์ `^\d{4}-`) ถ้าฝั่ง FRONT เริ่มนับ 0001 ใหม่
ความคืบหน้าของสองฝั่งจะทับกันทันที

แต่ละกลุ่มมีเลข `d` ประจำตัว: ฝั่ง BACK ใช้เลขวัน (1–9) · ฝั่ง FRONT ใช้ 101, 102
**เลขนี้ถูกเขียนไว้สองที่** (`FRONT` ใน `assets/nav.js` และ `FE_GROUP_ID` ใน
`tools/build-search-index.py`) — เพิ่มกลุ่มใหม่ต้องแก้ทั้งคู่ ถ้าลืม ตัวสร้าง index
จะหยุดพร้อมบอกชื่อกลุ่มที่ขาด

**คีย์ `localStorage` ของเว็บนี้:**

| คีย์ | เก็บอะไร | ใครแตะ |
|---|---|---|
| `go-course-progress-v1` | บทที่ทำเสร็จแล้ว | `progress.js` **และ** `quiz.js` — แก้ที่ไหนต้องแก้ทั้งสองที่ |
| `go-course-track-v1` | ฝั่งที่เลือกไว้ล่าสุด (ใช้เฉพาะตอนอยู่หน้าที่ไม่ใช่บทเรียน) | `nav.js` |

**คีย์ความคืบหน้า: `go-course-progress-v1`**
คะแนน quiz **ไม่ถูกบันทึก** ตั้งใจให้ตอบใหม่ได้ทุกครั้งที่กลับมาทบทวน
ป้ายที่มี `data-progress-track="back|front"` ใน `index.html` จะนับเฉพาะแถวที่
`data-track` ตรงกัน — เพิ่มฝั่งใหม่ไม่ต้องแก้ `progress.js`

---

## 5 · ทางเดินของข้อมูล

**เส้นที่คนเขียนเอง**

```
assets/nav.js  (DATA ที่บล็อกบนสุด)  ──►  แถบนำทาง + dropdown ของแต่ละวัน
```

**เส้นที่เครื่องสร้างให้ — ต้องสั่งรันเอง ไม่มีใครรันให้ตอน deploy**

```
day-*/**.html  และ  fe-*/**.html
    │
    │  python3 tools/add-heading-ids.py        แปะ id ลงไฟล์ HTML จริง
    ▼
(ไฟล์เดิม มี id แล้ว)
    │
    │  python3 tools/build-search-index.py
    ▼
assets/search-index.json
    │
    │  โหลดตอนโฟกัสช่องค้นหาครั้งแรกเท่านั้น
    ▼
assets/nav.js  ส่วนที่ 4.5  (UI ค้นหา)
```

คลังคำไม่ได้ index เนื้อหาเต็ม (จะได้ 4.9 MB) แต่เก็บ 4 ชนิด:
ศัพท์จาก `<dt>` ในกล่อง `.jargon` · หัวข้อ h2/h3 · ชื่อบท · โค้ด inline ที่โผล่ ≥2 ครั้ง
รวม 3,428 รายการ = 397 KB (gzip 96 KB)

ranking แบ่งเป็น **ชั้น** (ตรงเป๊ะ / ขึ้นต้น / อยู่กลาง / อยู่ในคำอธิบาย) ห่างกันชั้นละ 1000
แล้วค่อยบวกโบนัสตามชนิด (ศัพท์ 480 · บท 320 · หัวข้อ 160 · โค้ด 0)
→ ในชั้นเดียวกันศัพท์ชนะโค้ดเสมอ แต่ **ข้ามชั้นไม่ได้**

> ⚠️ **ห้ามใช้ `innerHTML` ในส่วนค้นหาเด็ดขาด** ข้อความในคลังมาจาก HTML ของบทเรียน
> มี `<` `&` และเครื่องหมายคำพูดปนอยู่หลายร้อยรายการ · ตอนนี้ประกอบด้วย DOM API ล้วน

---

## 6 · ตำแหน่งของ skill `/teach` ในรีโปนี้

รีโปนี้คือ **teaching workspace** ตามสัญญาของ skill `/teach`
(`~/.claude/skills/teach/SKILL.md`) ซึ่งกำหนดว่าสถานะการเรียนอยู่ในไฟล์เหล่านี้:

| ไฟล์ตามสัญญาของ `/teach` | ในรีโปนี้ | ใช้ทำอะไรจริง |
|---|---|---|
| `MISSION.md` | มี | ตัดสินว่า "จะสอนอะไรต่อ" · ทุกบทต้องผูกกลับมาที่นี่ได้ |
| `RESOURCES.md` | มี | แหล่งอ้างอิงที่ตรวจแล้ว — ใช้เขียนหัวข้อ "อ้างอิง" ท้ายบท |
| `learning-records/*.md` | มี | บันทึกสิ่งที่เรียนรู้ + เหตุผลของการตัดสินใจ · ใช้ประเมิน ZPD |
| `lessons/*.html` | แยกเป็น `day-N/lessons/` | หน่วยหลักของการสอน |
| `reference/*.html` | แยกเป็น `day-N/reference/` | cheat sheet รายวัน |
| `assets/*` | มี | component ที่ใช้ซ้ำ |
| `NOTES.md` | มี | preference ของผู้เรียน + บันทึกการทำงาน |
| `GLOSSARY.md` | เพิ่มเอง | ศัพท์ที่ต้องใช้ให้ตรงกันทุกบท |

**ที่ทำต่างจาก skill โดยตั้งใจ:**

- **แยกโฟลเดอร์ตามวัน** แต่ **เลขบทต่อเนื่องข้ามวัน** (`0001`–`0055`)
  เพราะคอร์สมาเป็นวัน ๆ แต่ผู้เรียนอ้างถึงบทด้วยเลขเดียวโดด ๆ
- **มี `index.html` เป็นประตูเดียว** — skill ไม่ได้กำหนดไว้ แต่ผู้เรียนขอเอง
- **เว็บนี้ deploy จริงให้เพื่อนร่วมคอร์สใช้** ไม่ใช่ไฟล์ส่วนตัวอีกแล้ว
  → ทุกหน้าต้องเปิดจาก URL ตรง ๆ ได้ ไม่ใช่ต้องไล่จากหน้าแรก
- **บทเรียนสรุปจากสไลด์ของคอร์สจริง** ไม่ใช่หัวข้อที่คิดขึ้นเอง
  → ZPD ถูกกำหนดโดยจังหวะของคอร์ส ไม่ใช่โดยเราล้วน ๆ

> **`/teach` เป็น skill ที่เรียกด้วยมือเท่านั้น** (`disable-model-invocation: true`
> ในตัว skill เอง และกฎ "skills are manual-only" ใน CLAUDE.md ส่วนตัวของผู้ใช้)
> — ทำงานตามเอกสารนี้ได้เลย ไม่ต้องเรียก skill ถ้าผู้ใช้ไม่ได้สั่ง

---

## 7 · หลักการออกแบบบทเรียน (design logic ของเนื้อหา)

### 7.1 ลำดับ 3 ขั้น ที่ใช้กับทุกเรื่องที่สอน

```
อธิบายเป็นภาษาคนก่อน  →  .analogy เทียบให้เห็นภาพ  →  .jargon ติดป้ายศัพท์
```

**ห้ามสลับลำดับ** — ติดป้ายศัพท์ก่อนอธิบาย = ผู้เรียนจำคำได้แต่ไม่รู้ว่ามันคืออะไร

- `.analogy` **ต้องมีท่อน "ที่ไม่เหมือนกัน:" เสมอ** — ทุกการเปรียบเทียบมีจุดที่มันโกหก
  ถ้าไม่บอก ผู้เรียนจะเอาส่วนที่โกหกไปใช้ต่อ
- `.jargon` มีไว้ให้ผู้เรียน **เอาไปคุยกับ senior หรือ AI ได้** →
  ทุก `<dd>` ควรมีประโยคที่พิมพ์ส่งได้จริง เช่น "ถาม senior ได้ว่า…" / "พิมพ์บอก AI ได้ว่า…"
- เกณฑ์: จำนวน `.jargon` ต้อง **ไม่น้อยกว่า** `.analogy` และอย่างน้อย 2 กล่องต่อบท

### 7.2 องค์ประกอบขั้นต่ำของหนึ่งบท

- predict-the-output อย่างน้อย 1 จุด
- quiz 4 ข้อ
- ภารกิจให้ลงมือทำ 1 ชุด — **ตั้งแต่บท 19 เป็นต้นไปต้องลงท้ายด้วย "เขียนเทสพิสูจน์"**
  ไม่ใช่ "ลองรันดู" · **ตั้งแต่บท 37 บทที่มี handler ต้องพิสูจน์ด้วย `httptest`**
- แหล่งอ้างอิงหลักอย่างน้อย 1 แหล่ง พร้อมบอกว่า **ให้อ่านตรงไหน**
- คำเชิญให้ถามกลับ (ตัวแทนของ "agent คือครูของคุณ" ในสัญญาของ `/teach`)
- ความยาว ~15 นาที เป็นเป้า (บทที่หนักจริงยืดได้ถึง ~25 แต่ต้องบอกไว้ใน `.meta`)

### 7.3 เกณฑ์แบ่งบท — แบ่งตาม "กับดัก" ไม่ใช่ตามหัวข้อสไลด์

หัวข้อในสไลด์หนึ่งหัวข้ออาจกลายเป็น 3 บท หรือ 3 หัวข้อยุบเป็นบทเดียวก็ได้
**หน่วยที่ใช้แบ่งคือ "จำนวนกับดักที่ต้องรันให้ดู"** (Day 6 ใช้เกณฑ์นี้ ได้ 9 บท บทละ 13–18 นาที)

กฎย่อยที่ตกผลึกแล้ว:
- สไลด์ที่เป็น **API surface ล้วน ๆ** (รายการ method/flag/assert) →
  ยกทั้งรายการไป cheat sheet แล้วให้บทเรียนโฟกัสที่ **"เลือกอันไหนเมื่อไหร่"**
- **เนื้อหาที่คนละธรรมชาติ ห้ามปนกันในบทเดียว** — Day 8 แยกเป็น
  บท 33–36 ที่ไม่มีโค้ด Go เลย (REST · status code · กฎตั้งชื่อ) กับบท 37–42 ที่เป็นโค้ดล้วน
  ถ้าปน ผู้เรียนจะจำ API ของ framework ได้แต่ยังออกแบบ endpoint ไม่เป็น
- **สอนของมาตรฐานให้จบก่อนแตะ framework เสมอ** แม้สไลด์จะข้าม —
  บท 38 (Gin) เขียนเป็น**การรีแฟกเตอร์โค้ด `net/http` ของบท 37 ทีละบรรทัด**
  ไม่ใช่บทสอน Gin ตั้งแต่ศูนย์ · ไม่งั้นผู้เรียนไม่มีวันรู้ว่า framework ทำอะไรให้
- **โค้ดฝั่ง "วิธีเก่าที่แย่" ในสไลด์ต้องรันด้วยเสมอ** — มันมักกลายเป็น
  predict-the-output ที่ดีที่สุดของบท

---

## 8 · Pipeline: สไลด์หนึ่งวัน → บทเรียน

### ขั้นที่ 0 — รับไฟล์

PDF เข้า `from_teacher/` ซึ่ง **gitignore ไว้** (สื่อของ PEA DevPool · ~58 MB)
**ห้าม commit ห้ามลิงก์ถึงจากหน้าเว็บ**

### ขั้นที่ 1 — อ่านสไลด์เป็นข้อความ

```bash
pdftotext -layout from_teacher/<file>.pdf -
```

### ขั้นที่ 2 — ถ้าเป็นฉบับ (Post) ให้ diff กับฉบับ (Pre) ทีละหน้า

ผู้สอนส่งสไลด์มาสองรอบเสมอ: ก่อนเรียน (Pre) และหลังเรียน (Post) ที่แก้/เพิ่มแล้ว

วิธีที่ใช้ได้จริง: `pdftotext -layout` ทั้งสองไฟล์ → จับคู่หน้าด้วย
**บรรทัดแรกที่ไม่ว่างของแต่ละหน้า** → diff เนื้อหาที่ normalize แล้ว
**อย่าเทียบด้วยจำนวนหน้า** — จำนวนหน้าลดลงได้ทั้งที่เนื้อหาเพิ่ม (เขาจัดหน้าใหม่)
สัญญาณที่เชื่อได้คือ **ขนาดตัวอักษรรวมต่อหน้าที่เปลี่ยนไป**

### ขั้นที่ 2.5 — ถ้าสไลด์เป็นภาพเกือบทั้งหมด ให้แปลงเป็นรูปทีละหน้า

สไลด์ฝั่ง Frontend เป็นแบบนี้ทั้งสองไฟล์: `pdftotext` ได้แค่หัวข้อกับ bullet เดียวต่อหน้า
(74 หน้าได้ข้อความแค่ 15 KB) — **โค้ดตัวอย่างทั้งหมดอยู่ในรูป**

```bash
pdftoppm -jpeg -r 100 -jpegopt quality=72 "ไฟล์.pdf" <ที่เก็บ>/p
```

ได้ `p-01.jpg` … เปิดอ่านด้วย Read tool ได้เลย · **ห้ามเดาเนื้อหาจากชื่อหัวข้อ**
สัญญาณว่าต้องทำขั้นนี้: ขนาดข้อความที่แกะได้ **น้อยกว่า ~500 ไบต์ต่อหน้า**

### ขั้นที่ 3 — คัดหน้าที่ซ้ำกับบทที่มีอยู่แล้วทิ้ง

Day 7 · Day 8 · Day 9 ต่างก็มีครึ่งแรกที่ซ้ำกับวันก่อน — **ข้าม อย่าเขียนซ้ำ**
ของที่เหลือนิดหน่อยให้ยกไปเติมใน cheat sheet แทนการทำเป็นบท

### ขั้นที่ 4 — รันทุกอย่างที่จะอ้างเป็นผลรัน

- โค้ด Go: รันบน Go เวอร์ชันจริง แล้ว **จดเวอร์ชันลงในกล่อง `.note` ต้นบท**
- ของที่ต้องใช้ container: `docker run` จริง แล้ว **ลบ container ทิ้งเมื่อเสร็จ**
  (เครื่องนี้ใช้ `colima` · ใช้ port สูง ๆ เช่น 55432 กันชนกับของผู้เรียน)
- ⚠️ **เปลี่ยนทีละตัวแปรเวลาจะสรุปว่าอะไรเป็นเหตุ** — เคยสรุปผิดว่า flag ตัวหนึ่งเป็นเหตุ
  เพราะทดสอบโดยใส่คู่กับอีกตัว · ผิดแบบนี้ขึ้นเว็บไปแล้วรอบหนึ่ง
- ⚠️ **เจอ config สองบรรทัดตีกัน ต้องรันสองรอบ** (เอา A ออก / เอา B ออก)
  ไม่ใช่พิสูจน์ว่าบรรทัดหนึ่งเป็น no-op แล้วหยุด

### ขั้นที่ 5 — เขียนไฟล์บทเรียน

ตามโครงในหัวข้อ 3 และ 7 · **คัดลอกโครงจากบทที่มีอยู่** อย่าเริ่มจากศูนย์
ชื่อไฟล์ต่อจากเลขล่าสุด — **เลขบทต่อเนื่องทั้งเว็บ ไม่รีเซ็ตตามฝั่ง**

| สไลด์กำกับวันมาให้ไหม | โฟลเดอร์ | ตัวอย่าง |
|---|---|---|
| กำกับ (ฝั่ง Backend) | `day-N/lessons/` | `day-9/lessons/0055-mid-exam-workshop.html` |
| ไม่ได้กำกับ (ฝั่ง Frontend) | `fe-<slug>/lessons/` โดย `<slug>` มาจากชื่อสไลด์ | `fe-html-css/lessons/0056-how-the-web-works.html` |

**อย่าตั้งวันเอง**ให้สไลด์ที่อาจารย์ไม่ได้กำกับวันมา — จะไปชนกับวันจริงที่ส่งมาทีหลัง

### ขั้นที่ 6 — cheat sheet ของวันนั้น

`day-N/reference/cheatsheet.html` ครอบคลุมสไลด์ทั้งวัน รวมของที่ตัดออกจากบทเรียน
ออกแบบให้พิมพ์ลงกระดาษได้

### ขั้นที่ 7 — ต่อสายให้ครบ (จุดที่ลืมบ่อยที่สุด)

1. `index.html` — เพิ่ม `<section class="day">` + `<li class="lesson" data-id="NNNN">`
   + แก้ตัวเลขรวมใน `.progress-summary` + แก้การ์ด `.upcoming` เป็นวันถัดไป
2. `nav` ท้ายบทสุดท้ายของวันก่อนหน้า → ชี้มาบทแรกของวันใหม่
3. **`DATA` ใน `assets/nav.js`** (`DAYS` สำหรับฝั่ง BACK · `FRONT` สำหรับฝั่ง FRONT) —
   ถ้าลืม บทใหม่จะเปิดได้จากลิงก์ตรงเท่านั้น
   **แต่จะไม่โผล่ในเมนูเลย และไม่มีอะไรฟ้อง** เพราะไม่ใช่ลิงก์เสีย
   · กลุ่มใหม่ต้องแจกเลข `d` ด้วย และเลขนั้นต้องไปใส่ใน `FE_GROUP_ID` ของ
   `tools/build-search-index.py` ให้ตรงกัน
4. ทุกหน้าใหม่ต้องมี `<script src="../../assets/nav.js" defer></script>` ใน `<head>`

### ขั้นที่ 8 — รันตัวสร้าง

```bash
python3 tools/add-heading-ids.py
python3 tools/build-search-index.py
```

ตามลำดับนี้เท่านั้น (ตัวหลังอ่าน id ที่ตัวแรกแปะ) · ทั้งคู่ idempotent

### ขั้นที่ 9 — ตรวจก่อน commit

ดูหัวข้อ 9 ข้างล่าง

### ขั้นที่ 10 — learning record + push

เขียน `learning-records/NNNN-*.md` บอกว่ารอบนี้ตัดสินใจอะไรและเพราะอะไร
แล้วเพิ่มหนึ่งบรรทัดในอาร์เรย์ `DOCS` ของ `docs.html`
จากนั้น `git add . && git commit && git push` — **ผู้เรียนอนุญาตไว้ล่วงหน้าแล้ว ไม่ต้องถาม**

---

## 9 · การตรวจก่อนขึ้นเว็บ

| ตรวจอะไร | ทำยังไง |
|---|---|
| ลิงก์ภายในไม่เสีย | ไล่ทุก `href` ในทุกไฟล์ · **ต้องตรวจ `?f=` ของ `docs.html` ด้วย** ไม่ใช่แค่ path |
| แท็กปิดครบ · `data-answer` ไม่เกินจำนวนตัวเลือก | สคริปต์กวาดทั้งรีโป |
| layout ไม่ล้น | เสิร์ฟทั้งโฟลเดอร์ → โหลดทุกหน้าใส่ `<iframe>` → เทียบ `getBoundingClientRect()` ของลูกกับแม่ · **ต้องรันหลายความกว้าง (380 / 768 / 1280)** |
| ไม่มีขยะหลุดเข้ารีโป | ดูไฟล์ที่ยัง untracked ด้วย `git status --porcelain` และ container ที่ลืมลบด้วย `docker ps -aq` |
| คลังคำค้นตรงกับไฟล์จริง | `python3 tools/build-search-index.py --check` |

**กฎเหล็กสองข้อของการตรวจ:**

> **1. ก่อนเชื่อว่า "ตรวจแล้วไม่เจอ" ต้องเอาบั๊กกลับมาใส่แล้วดูว่าตัวตรวจร้องไหม**
> ไม่งั้นแยกไม่ออกระหว่าง "ไม่มีปัญหา" กับ "เครื่องมือเสีย" ·
> เคยพลาดมาแล้ว: sweep รายงาน 0 จุดทั้งที่บั๊กอยู่ในไฟล์จริง เพราะ **browser cache**
> → ต้องเสิร์ฟด้วย `Cache-Control: no-store` และตัด `Last-Modified`/`ETag` ทิ้ง
>
> **2. ตรวจซ้ำให้กวาดทั้งบท อย่าตรวจเฉพาะจุดที่สงสัย**
> รอบตรวจบท 30–32 จุดที่สงสัยไว้ 21 จุดไม่ผิดสักจุด
> แต่พอกวาดทั้ง 72 บล็อก เจอข้อผิดพลาดเชิงการสอน 1 จุด**ที่อยู่นอกชุดที่สงสัย**

`getBoundingClientRect()` ของ inline element คือกรอบรวมของทุกบรรทัดที่มันพาดผ่าน →
detector ต้อง **ข้ามเมื่อ `display` ของตัวเองหรือของพ่อขึ้นต้นด้วย `inline`**
ไม่งั้นได้ false positive เป็นพัน

---

## 10 · Deploy

repo: <https://github.com/Thuwanon01/learnGo> · push ขึ้น `main` แล้ว Vercel deploy อัตโนมัติ
branch อื่นได้ Preview URL ของตัวเอง · Framework Preset = **Other** · Build Command เว้นว่าง

`vercel.json` กำหนดแค่สองเรื่อง: cache header · และเสิร์ฟ `.md` เป็น
`text/plain; charset=utf-8` (ไม่งั้นเบราว์เซอร์ดาวน์โหลดไฟล์แทนที่จะเปิดอ่าน)

- **`cleanUrls: false` โดยตั้งใจ** — ลิงก์ภายในเกือบ 800 เส้นลงท้าย `.html`
  ถ้าเปิด clean URL ทุกลิงก์จะโดน redirect 308 ก่อนเสมอ ช้าโดยไม่ได้อะไร
- **`404.html` ต้องใช้ path แบบ `/assets/...`** เพราะ Vercel เสิร์ฟหน้านี้จาก URL ไหนก็ได้
  รวมถึง path ลึก ๆ · relative path จะพังทันที
- `docs.html` ใช้ `fetch()` จึงเปิดด้วย `file://` ไม่ได้ (CORS) —
  ในเครื่องให้รัน `python3 -m http.server 8000` ก่อน
