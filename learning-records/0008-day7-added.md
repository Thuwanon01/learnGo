# Day 7 — วันแรกที่ต้องข้ามเนื้อหาซ้ำ และวันแรกที่ใช้ subagent ตรวจของจริง

เพิ่มบทเรียน 28–32 (ห้าบท) + cheat sheet Day 7
**ครึ่งแรกของสไลด์ถูกข้ามทั้งดุ้นเพราะซ้ำกับบทเรียน 27**

## การข้ามเนื้อหาซ้ำ — ตัดสินยังไง

ผู้เรียนสั่งมาเองว่า "บางเรื่องซ้ำ ถ้าอันไหนซ้ำก็ข้ามไปเลย"
วิธีที่ใช้: ดึง outline ของสไลด์ออกมาแล้ว **เทียบกับหัวข้อ `<h2>` ของบทเรียนที่มีอยู่**
ด้วย grep ไม่ใช่เทียบด้วยความจำ

ผลคือหัวข้อแรกทั้งก้อน (Basic RDBMS: Table / PK / FK / SQL / Transaction ·
`compose.yml` `postgres:17` · TablePlus · GORM model tag · CRUD ทั้งสี่)
ตรงกับบทเรียน 27 หัวข้อ 4, 6, 7 **แบบเกือบคำต่อคำ** จึงข้ามทั้งหมด

**สิ่งที่ทำแทนการข้ามเงียบ ๆ:** เขียนบอกไว้ในการ์ด Day 7 บนหน้าแรกว่าข้ามอะไรไป
เพราะอะไร — ผู้เรียนที่ถือสไลด์อยู่ในมือจะได้ไม่สงสัยว่าทำไมบทเรียนไม่ตรงกับสไลด์

## ใช้ workflow กระจายงาน — ผลจริงเป็นยังไง

โครงที่ใช้: **Verify (4 agent รันของจริงคู่ขนาน) → Write (5 agent เขียนบทเรียน) → Cheat sheet**

**ที่ได้ผลดีมาก:** ให้ agent ติดตั้งเครื่องมือจริงแล้ว *สั่งให้ตั้งใจทำให้พัง*
ได้กับดักที่พิสูจน์แล้วรวม 55 ข้อ พร้อม error verbatim — มากกว่าที่คนเดียวจะทำทันในวันเดียว
ของที่ได้มาแบบนี้เอาไปทำ predict-the-output ได้ตรง ๆ

**บั๊กที่ตัวเองทำ:** ประกาศตัวแปร `LESSONS` ไม่ครบในสคริปต์ → เฟส Verify วิ่งจบสวยงาม
แต่เฟส Write พังทั้ง 4 กลุ่มด้วย `LESSONS is not defined`
**สิ่งที่ช่วยไว้คือ resume** — แก้สคริปต์แล้ว resume จาก runId เดิม
ผลตรวจที่แพงที่สุด (ติดตั้ง + docker) ถูกดึงจาก cache ไม่ต้องรันซ้ำ
→ **บทเรียน: ตรวจว่าตัวแปรทุกตัวถูกประกาศก่อนยิง workflow ยาว ๆ**

## เรื่องที่สำคัญที่สุดของวันนี้ — subagent ตัดสิน "สไลด์ผิด" ไม่ได้

agent รายงานกลับมาว่าเจอสไลด์ผิด **21 จุด** · คัดกรองเองแล้วเหลือ **5 จุด**

| ประเภทที่ตัดออก | ตัวอย่าง |
|---|---|
| สไลด์ไม่ได้เขียนผิด แต่ agent ตีความเกิน | "Redis เก็บใน RAM" — สไลด์เขียน *"เป็นหลัก"* ไว้แล้ว |
| ช่องว่างของสไลด์ ไม่ใช่ความผิด | สไลด์ไม่ได้บอกเรื่อง `GOOSE_DRIVER` / `DOCKER_HOST` |
| **agent อ่านสไลด์คลาดเคลื่อนเอง** | อ้างว่าสไลด์ตั้ง `all` ผิดระดับ ทั้งที่สไลด์ถูกอยู่แล้ว |

**สาเหตุ:** agent เห็นแต่ผลรันของตัวเอง มันไม่ได้อ่านสไลด์ทั้งหน้า
มันจึงเก่งเรื่อง *"ของจริงทำงานยังไง"* แต่ตัดสิน *"สไลด์พูดว่าอะไรกันแน่"* ไม่ได้

**กฎใหม่ของ workspace:** ผลตรวจจาก subagent เอาไปใส่ learning record ไม่ได้ทันที
ต้องเอามาเทียบสไลด์เองก่อนเสมอ · ทั้ง 5 จุดที่บันทึกไว้ รันซ้ำด้วยมือเองทุกจุด

## สิ่งที่ตรวจแล้วเปลี่ยนวิธีเขียนบท

| # | สิ่งที่พบ | เอาไปทำอะไร |
|---|---|---|
| 1 | mock เขียนมือ **PASS** ทั้งที่ส่ง argument ผิด แต่ mock ของ mockery **FAIL** | predict คู่ของบท 28 — วางเคสเดียวกันเทียบกันตรง ๆ |
| 2 | unit test ที่ mock ผ่านฉลุยทั้งที่ SQL อ้าง column ที่ไม่มีจริง (`SQLSTATE 42703`) | predict หลักของบท 29 — เป็นเหตุผลทั้งหมดที่ integration test มีอยู่ |
| 3 | GORM `Find` คืน slice ที่ **ไม่ nil** (JSON ได้ `[]`) แต่ `Raw().Scan` คืน nil (ได้ `null`) | แก้ความเชื่อจากสไลด์ที่บอกว่าต้อง `make([]T, 0)` เสมอ |
| 4 | container ต่อหนึ่งเทส 4.264s → `sync.Once` เหลือ 1.727s | ตัวเลขที่ทำให้ `sync.Once` เป็นเรื่องจำเป็น ไม่ใช่ลูกเล่น |
| 5 | boot container ใน `TestMain` ก่อน `m.Run()` ทำให้ `-short` **ไม่ช่วยอะไรเลย** | กับดักที่คนอ่านสไลด์จะเขียนตามโดยธรรมชาติ |
| 6 | `goose_db_version` **ไม่มี checksum** — แก้ไฟล์ที่ `up` ไปแล้ว goose ไม่รู้ | predict หลักบท 31 · เป็นบั๊กที่ทำ production พังจริง |
| 7 | `AutoMigrate` rename column ไม่ได้ (สร้างคอลัมน์ใหม่ ข้อมูลไม่ตามมา) และ drop ไม่ได้ | เหตุผลที่ต้องมี goose ทั้งที่บท 27 สอน AutoMigrate ไปแล้ว |
| 8 | `TTL` คืน `-1` (ไม่มีวันหมดอายุ) กับ `-2` (ไม่มี key) — คนละความหมาย | ตารางสั้น ๆ ในบท 32 · มือใหม่สับสนสองค่านี้ตลอด |
| 9 | cache ที่ไม่ invalidate อ่านได้ค่าเก่าจริง | predict หลักบท 32 — หัวใจของบท |

## ช่องว่างที่ปิดไปแล้ว และที่ยังเหลือ

Day 7 ปิด backlog ข้อ 3 (**เขียนเทสให้ repository ที่ต่อ database จริง**) ได้เต็ม ๆ
ผ่านบท 29–30 ซึ่งเป็นเรื่องที่รอมาตั้งแต่ Day 5

**ที่ยังไม่มีและตอนนี้กลายเป็นช่องว่างเดียวที่ใหญ่จริง:** `net/http` + routing +
`httptest` + middleware — สไลด์ยังไม่แตะเลย ทั้งที่บท 27 มี `handler.go` โผล่มาแล้ว
